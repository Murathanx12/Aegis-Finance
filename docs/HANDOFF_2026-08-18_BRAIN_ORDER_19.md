# ORDER — brain → builder (order 19) — the freeze applies to the frozen surface, not the repo

Binding, short. Order 18 delivered in full and the judgment calls in it were
right — three deserve saying so before the next instruction:

- **Amihud annotated, not amputated.** Refusing to repeat the Roll fix
  mechanically — because Amihud is a ratio of observables and AAPL ≈ 0 is a
  true statement, not a failed estimate — is the "refusal installed without
  checking where the live inputs fall" trap avoided by its own author on the
  same day it was named. The pinned 0.30 weight so nobody "finishes the job"
  is the right lock.
- **Kyle's empty range reported as a refusal**, where a naive profiler would
  have recorded "no floor." That distinction is the whole sweep.
- **Not wiring `finished_before_open` into `run_night()` 80 minutes before a
  launch.** Correct, and stays correct — tomorrow it is closed the honest way,
  reading receipts after the fact.

## 1. RULING — stopping was right for the IIF surface and wrong as a general halt

Night 2 runs from `iif1_frozen`, cut at `42029b6` precisely so that main can
change all day without changing the experiment. Therefore:

> **The pre-launch freeze applies to the FROZEN SURFACE, not the repo.** No
> commit to the night path, the launcher, or anything the manifest pins, from
> now until the receipt is written. Everything else — the P1 builds, the
> episode libraries, the autopsy — continues through the launch and through
> the night. A launch that halts the factory is the coupling Order 17
> abolished; the worktree is what makes the decoupling real, so use it.

"Each P1 is a session" is true and is an argument for STARTING one this
evening, not for waiting — nothing half-built on main can contaminate a night
that runs from a worktree pinned by a manifest.

## 2. RULING — the BAR is economic, and an unanswerable cell is recorded, never rescued

The Order 18 finding that at BAR = 0.10 the 40-night read cannot answer
`h=1 | thr 0.03` is handled by §64, not by redesign:

- **The bar is declared on economic grounds and is never shrunk to make a
  cell answerable.** Choosing the margin to bless the design is §37 wearing
  an equivalence test — already canon.
- If the honest bar leaves `h=1` underpowered at 40 nights, the declaration
  SAYS SO at reservation: that cell's read will return `NOT_ANSWERABLE_AT_N`,
  and the campaign's answerable surface is the remaining cells. Recorded now,
  before any outcome, is exactly what §64 is for.
- **No mid-flight redesign.** Extending nights or reweighting cells is a
  registered-trial amendment — attended, and almost certainly not worth it.
- Draft for Murat's signature: the bar, its economic justification in two
  sentences, and the per-cell answerability table. That is the whole document.

## 3. The evening, through and past the launch

1. **17:00 — Murat launches Night 2 attended** from the frozen worktree,
   `--dry-run` immediately before. (READY TO LAUNCH already confirmed,
   margin +85.)
2. **Kick off the CONVEXITY-PRESERVATION-1 episode-library construction as a
   long-running background build** — +20/+40/+75/+100 thresholds, matched
   losers, PIT features at each threshold. It is compute-shaped for a night
   and touches nothing the manifest pins.
3. **Spend the session on the autopsy's next cut.** "The 14-point gap is
   EQUAL WEIGHT, not HRP" changes the question: cross the arms. Replay
   conviction's book under the mirror's weighting and the mirror's book under
   conviction's sizing, one difference at a time, so the gap decomposes into
   scheme vs selection vs timing with a number on each. This is the highest
   information-per-hour work available tonight and it is about Murat's own
   money.
4. **19:00 — check the prod-monitor's first real firing** (its window opens
   then; the email fix is unverified until it fires).
5. Tomorrow, fresh: `finished_before_open` closed receipts-side, the
   remaining write-only fields triaged (167 of 806 is a backlog, not an
   evening), and the next P1.

## 4. Standing

- **The pre-launch freeze applies to the frozen surface, not the repo.** A
  launch that halts the factory recreates the coupling the worktree exists
  to break.
- **A bar is never shrunk to make a cell answerable; an unanswerable cell is
  recorded at reservation.**
- **An instrument is annotated or amputated by where its LIVE inputs fall,
  never by analogy to the last instrument fixed.**

— brain, 2026-08-18
