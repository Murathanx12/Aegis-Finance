# Code facts gathered 2026-09-25 (Sonnet Explore, verbatim signatures) — for the builders

- `llm_portfolio.build_briefing(*, asof=None, max_names=4000, bars=None) -> dict` (line 120); rows carry `t, px, band, cost_bps, dollar_vol_m, mom_*, vol_63, beta_63, vs_52w_high, vs_ma200, drawdown_63, skew_63, amihud` + fundamentals `rev_qoq, rev_yoy, gross_margin, gross_margin_chg, last_filed, inflection_flag`; `briefing_hash`.
- `llm_portfolio.freeze(book, *, briefing=None, today=None) -> dict` (231): refuses empty name/objective/positions, duplicates, shorts, weight sum outside 0.98-1.02; records `max_weight` but does NOT cap it; does NOT check tickers against a panel. `book_id = _hash({name, positions, asof})`. Books append to `<OPTIMUS_LEDGER_DIR>/llm_portfolio/books.jsonl` (`append_book`, `read_books`, `books_path`).
- `llm_portfolio.grade(rec, bars, *, today=None) -> dict` (326): entry at the OPEN of the session after `asof`; horizons `HORIZON_DAYS=(1,5,21,126)`; entry cost = half round-trip by band via `XR.round_trip_bps`; SPY from the same bars. CLI `scripts/llm_portfolio.py`: `brief|freeze <file>|grade|template`; grade receipt `llm_portfolio/grade_<date>.json`; bars = `XR.load_bars(XR.survivorship_free_paths())` (US only).
- No `contracts/` dir under `first_books/`; twins exist for PaperBook strategies via `paper_books.make_twins(strategy, *, cadence, bars=None, asof=None)`; NOT for llm_portfolio books.
- `scripts/sim_run.py`: units registered in `run()` as `c.unit(name, fn)` in order reconcile, funnel, analyst, rank, plan, grade, learn (lines 732-746); `u_grade() -> dict` (536) calls `forecast_grader.grade_due()` and `decision_ledger.score_due()`; `u_learn(cycle_n, out)` (552) rota `("survivorship_audit","breadth_check","idle")` at ~570. Heartbeat: `sim_session.heartbeat(cycle=None, note=None)`; `Cycle.unit` beats before the unit and a daemon thread beats every `IDLE_BEAT_S=60` during it; `MIN_CYCLE_PERIOD_S=300`; `SLOW_UNIT_S=1800`. Receipts `<OPTIMUS_LEDGER_DIR>/sim/<session_id>/cycle_NNNN.json`; `sim_session.STATE_DIR/SESSION_PATH/STOP_FLAG/HISTORY_PATH(sessions.jsonl)`; `STALE_AFTER_S=300`.
- `scripts/night_investigator_forecast.py`: `--n 20 --model deepseek/deepseek-flash --dry-run`; tickers = most 90d revision activity in `<OPTIMUS_LEDGER_DIR>/analyst/target_revisions.parquet` ∩ eligible panel; writes via `belief_state.make_prediction` with `specialist="investigator:evidence_v2"`, `horizon_days=1`, `probability` (shrunk 0.65 toward base), `resolves_after`, `thesis`, `counter_thesis`, `input_snapshot`, `licence`; cost NOT logged; calls `openclaw agent --message-file <f> --model <m>` as subprocess.
- `openclaw_client`: `health(*, probe_web=False) -> Health`, `agent(message_file, *, model="deepseek/deepseek-v4-pro", timeout=600) -> dict`, `browser(verb, *args, url=None, timeout=180)`, `profile()`, `check_url(url)`; `DENIED_DOMAINS` = brokerages/banks/payments only; reddit.com is probed in `health(probe_web=True)`; x.com not addressed. `scripts/openclaw_collector.py` writes `<OPTIMUS_LEDGER_DIR>/web_events/collector_<job>_<date>.json` and appends via `web_events.append(rows)`.
- `agency.py`: `review(book, *, bars=None, asof=None, ..., targets=None, typed_events=None, sensor=None) -> list[dict]` (1852) one call per holding with its forecast row on disk; `review_book(book, **kw) -> dict` (2013); `review_all(*, ..., origins=("human_text",)) -> dict` (2042) = the Morning's `agency_review`; `decide_label(probability, *, weight, max_single_name) -> (decision, why)` in {sell, trim, buy_more, hold}; `protect_first_pass(...)` at the close; `ESG_CATEGORIES["gambling"]` = BYD, CZR, DKNG, FLUT, LVS, MGM, PENN, RSI, WYNN (328-336). Personalities `TABLE` (141): preservation/balanced/aggressive/extreme_growth.
- Murat's book: `backend/data/murat_book.yaml` (as_of 2026-08-11, confirmed: false): AARD 1000, ABSI 600, AMSC 50, BHVN 300, DKNG 150, HUBS 10, KYTX 250, NTLA 250, PRCH 200, QUBT 300, SLDP 600, SOC 700; watchlist of 34 names; same seed in `backend/data/book_lanes.yaml` (mirror + conviction lanes). Decision log: `POST/GET /conviction/decision(s)`, `backend/data/conviction_decisions_snapshot.json`.
- `analyst_ledger.py` reads its OWN `backend/data/analyst_snapshots.jsonl`, not the parquet; `target_revisions(ticker)` there = deltas of snapshots. The parquet is read directly with pandas elsewhere.
- News corpus: `scripts/news_pull.py` → `backend/data/optimus/news_corpus/<source>/<date>.jsonl`, 18 sources, **75,889 rows, newest first_seen_utc 2026-09-25T05:18Z**; row schema `{source, first_seen_utc, published_utc, tz_source, url, title, body, lang, tickers[], entity_tags[], raw_id, pit_grade}`; by-ticker helper `book_signals.load_news_rows(session)` (alpaca_benzinga only).
- config: `OPTIMUS_LEDGER_DIR = DATA_DIR/"optimus"`; `LAB_DAILY_SPEND_CAP_USD = 3.00` (env `AEGIS_LAB_DAILY_SPEND_CAP_USD`); `llm_analyzer._DAILY_CAP=150` calls; no INVESTIGATOR_*/BOOK_* constants yet.
- No sim session running on 2026-09-25 afternoon (last `f3d54fa01531` ended 00:27Z, 99 cycles).
