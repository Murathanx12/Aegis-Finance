# 2026-09-20 — Sunday: the deploy, the cost cut, the Kelly flip, the night

- `research_alpaca_and_social_round5.md` (Sonnet): hackathon winners UNPUBLISHED (428 submissions, judging in progress); no new Alpaca event open; AlphaDesk's guardrails are weaker than ours; TAKE: Alpaca skills-library checklist format, StockTwits trending as an attention control; TEST: insider cluster-buy 0-1d post-disclosure (§46 licenses it); IGNORE: Revelio ($85k/yr), congress copy (45-day lag).
- Fleet deployed (terminal `71d5cc1` market-window wrapper); staging deleted; Railway $47.22/month read; PROFIT_ALLOCATOR_v2 active (`d1ea5a81`); YouTube key placed; the authority has not allocated on a weekday since 09-11 — Monday 16:30 ET is the test.

- `review_night_2026-09-19_to_20.md` — Fable, 09-20 08:30 HKT: the night reviewed after Murat closed the lab ("local simulation was what I asked"). Scoreboard first: five readable books $440,907, hack2 401, zero decisions on disk, zero forecasts graded, $2.08 of cloud spent since the top-up. Three loops were dead containers (CRLF wrapper, fixed `0878516`); the idle queue ran one job per lab instance; the price table is wrong; what would be more efficient and more profitable, in order.

- `spec_decision_engine_and_scenario_gym.md` (Sonnet) — the diagnosis with line numbers (§A: the engine refuses at gates that run BEFORE any ranking, and even a survivor is never ranked by return, because `expected_payoff` is a fixed string and `estimated_probability` is `None` by design), the ROI rule (§B), the counterfactual-twin scenario gym (§C, not built), and the `REVISED` ledger state (§D). **§B and §D BUILT the same day** — `backend/services/roi_rank.py`, `config.SIGNAL_MEASURED_RETURN`/`IC_ROI_RANKING`/`ROI_MIN_T`, `decision_contract.revise`, `decision_ledger`'s `REVISED` at rank 3.5 (roadmap §14.4d). Today's contract scores 0 of 43: three of the eight adapter signals have a per-name net read on a receipt that is actually in this checkout, and none of the three carries a t ≥ 2 on the *return*. §C waits on the local-only policy and on its own adoption rule being a code-enforced weight cap.

- **Chunk 18d — §C, the scenario gym, BUILT the same evening** (`scripts/night_scenario_gym.py` = `S2_scenario_gym`, stage `features`, box 90 min, resumable; 51 tests, no model, no `backend/data`). N fictional decision points (X_anon_gap's own masking + a seeded per-cell date shift, numbers verbatim), each asked six ways: real, good twin, bad twin, two SIGN-MATCHED control twins, and the shuffled digest. The twins are one appended sentence from a frozen hashed library keyed by the dominant typed event's `direction_prior` — no model writes a twin. **The smoke run changed the design:** with one favourable control the bad-twin lift read +52.6 pp (t 3.89); against a sign-matched adverse control it is **+23.7 pp (t 1.82)**, and the good twin **+5.3 pp (t 0.53)** — half the apparent gut was tone-following. 120 calls, 127 s, $0.00, zero schema refusals; the return leg refuses (60% HOLD/CASH, 8 blocks, block MDE ±235 pp/yr). `adoption: NOT_ADOPTED — reliability_weight 0 until N>=300 and a forward record exists`, and that cap is a test, not an intention. Roadmap §14.4e.
