# Adjudication of `research_notes/2026-09-26/audit_forecasts_and_learning_since_august.md` — Fable, 2026-09-26 04:00 HKT

| # | audit finding | verdict | action |
|---|---|---|---|
| 1 | Accrual stopped 08-28 → 09-24 and nothing went red; grading ran twice, late; ~600 rows lost to the reset | **accepted, already fixed** (accrual canary; `u_forecast` daily; `u_grade` every cycle) | none new |
| 2 | **The investigator's skill is MAGNITUDE (+5.6% h=1); on direction (`return_sign` h=5) the same arms are −8.7% held out; the 0.65 shrink was earned on magnitude and applied to direction rows** | **accepted — the second time this week the same fact surfaced (reviewer row 2)** | `night_investigator_forecast`: store `raw_probability` as a FIELD, stop applying the magnitude-earned shrink to direction rows (record `shrink_basis`), let `forecast_reputation` recalibrate direction from raw once it has n; E[r]'s `investigator_dir` stays asleep until a direction calibration exists (it already is) |
| 3 | The five arms answered the same 312 questions on 8 dates, corr 0.70–0.93 ≈ 1.5 independent forecasters | **accepted** | the reputation receipt prints `n_independent_effective` beside n; no weight is quoted without it |
| 4 | `review:v0` writes p=0.50 on 383 of 385 rows (no power); `thesis_card:v1` 112 of 166 at 0.50 | **accepted as designed for now** | a neutral card / a HOLD label IS "no view"; the rows exist so that the day a label departs from 0.50 it is graded. The reviewer may argue the labels should carry the falsifier's direction. |
| 5 | Two green replays never ran forward: `forecast_dispersion_v1` +1.02%/mo t 5.65 (419 blocks, all eras); Book F seasonality +0.43%/mo t 3.12 | **accepted** | both handed to the strategy-library builder tonight with `green_replay_never_forward: true` → forward twin books regardless of rank |
| 6 | Fleet −9.8% / −$39k realised, 27.7% hit rate; the execution repo has had no commit since 09-20; its autopsy changed nothing | **accepted** | the fleet is the other repo's; the autopsy (`fleet_audit/trade_autopsy_2026-09-24.json`) is handed over in the handoff; nothing here arms or disarms it |
| 7 | **PROBE − REFUSED −0.68%/day (t −0.83, 3 blocks): the refusals beat the probes** | **accepted as the only decision grade so far; not a result at 3 blocks** | it is printed on the plan receipt daily; the PROBE gate keys to it at 21 sessions |
| 8 | Personality books buy 12-1 momentum daily (§59 closed) and can never be graded | **accepted** | retire them from the decision contract or give them a grade — reviewer's call next chunk; nothing new trades on them |
| 9 | **The learning layer learned nothing**: `LEARNED_2026-09.md` distilled 0 rules (local model not answering), `learned_rules.jsonl` absent, 126 heartbeat lines, `policy_state.json` never written | **accepted — the most damning line of the audit** | builder tonight: ExpeL distillation falls back to DeepSeek when the local model refuses (paired, recorded); `policy_state.json` is written every night with at least the graded-derived preferences (persona weights 0, reputation weights, the PROBE gate state), and a night that writes none is DEGRADED |
| 10 | 14 graded outcomes nobody reads (file + consumer named) | **accepted** | folded into chunk 3 (the timeline panel consumes them) and the reviewer's next pass |
| 11 | **Contract REFUSED ALLE (insider t 1.40 < 2.0), PC-PAPER bought 131 ALLE the same day** — two writers, one shortlist, two rules | **accepted — an accident, not an experiment** | builder tonight: `u_plan` PROBE excludes names the day's decision contract REFUSED with `NEGATIVE_EV`/`EDGE_BELOW_BAR`, and records `contract_clash: [names]` on the plan receipt; the contract's rows and the PROBE rows share one hypothesis id so one grade covers both |
| 12 | Calibration files rate both shortlist signals WEAK while the shortlist labels them supported | **accepted** | printed beside `n_considered` on the contract; the reviewer decides whether WEAK bars a PROBE |
| 13 | Cards rate CAPR/QUBT/BHVN/SLDP against while the review says HOLD and CAPR sits in five books | **accepted with a distinction** | HOLD is a *decision on a held position* (do not sell on a jump); AGAINST is a *view*; v2 removed CAPR and QUBT; the five books holding CAPR are frozen and stay frozen — that is what a frozen book means |
| 14 | `n_considered` still 2 after the funnel refresh | **accepted** | the stuck detector prints it; the cause (both survivors WEAK-calibrated) is the reviewer's next question |

Executed tonight: rows 5 (library), 2 + 11 (builder A: `sim_run`/`night_investigator_forecast`), 9 (builder B: the learning layer). Everything else is a spec line for the next chunk's reviewer.
