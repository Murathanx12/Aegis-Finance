# Adjudication — 2026-09-26 wave 1 (chunks A, B, D, F, G, H, I) — Fable, 17:30 HKT

Reviews: `REVIEW_2026-09-26_CHUNKS_H_I.md`, `REVIEW_2026-09-26_CHUNK_G.md`.
Builders' reports are in the session transcript; their commits: A `b827cd14`,
B `54a0735e`, D `c4f46332`, F `2140ba87`, G `cbe19f70`, I `2b06a390`, H (doc).

## The facts that bind everything after
| # | finding | source | verdict → action |
|---|---|---|---|
| 1 | **X is not logged in inside OpenClaw's `muratclaw` profile.** Every X quest tonight (A: 10, B: 7, F: 1) hit the login page. Murat's "I connected X" was in another browser. | A, B, F reports | **Murat**: log into X inside the OpenClaw browser profile (`openclaw browser open x.com` on `muratclaw`, sign in, leave the profile). Until then every X column is `NOT_READ_LOGIN_WALL`, never "no activity". |
| 2 | **No fast mover was predicted.** 318 cases over 1,135 positions; 62 favourable moves beat their controls; all accidental; PREDICTED_MECHANISM = 0. The +10% night books earned it on session 6 (NUAI +28.8%, AXTI +21.9%: news / an analyst cascade nobody saw at entry). | A | accepted as the honest answer to "why did they move" — luck and post-entry news; **no strategy is credited.** The template exists for the day a mechanism is seen at entry. |
| 3 | **A free trailing-63d-vol formula beats the LLM investigator on its own magnitude questions** (+10.2% vs +5.7% held out at h=1; wins 7 of 8 days). The LLM lowers skill from the vol prior. | reviewer H+I | accepted — **the one new result of the day.** `u_forecast`'s magnitude rows get the vol prior printed beside them; the learning report's "what works" must name the prior, not the LLM, until the LLM beats it on the six blocks resolving 09-29 → 10-05. |
| 4 | "614 of 1,041 modules unreachable" is an artefact (591 are test files; real 24, all explained). | reviewer H+I | accepted — fix the report's method (exclude `backend/tests`). |
| 5 | The learning report's EV constants are hand-set; sentence 3 chose an experiment that already runs; the honest P for direction calibration is 0.03-0.05 → EV ≈ $500. The reviewer's highest-EV experiment: **the same PROBE names weighted three ways (equal / inverse-vol / big-predicted-move tilt)**, ~$17k EV at $1M, separable from low-vol beta by regressing arm differences on USMV. | reviewer H+I | accepted — delete the hand-set table; the declared list carries measured spreads; **freeze the three PROBE-weighting twins tonight** (chunk E). |
| 6 | Library: 254 strategies, 762 cells, sealed split declared in code (32 sealed months, not 21); nothing reaches DSR 0.95; the sealed top-3 are one-regime rows (top-5-month share 0.9-1.4, dev CAGR ≈ 0); the only rows good in dev AND sealed are `mom_12_1_q` and the new `skill_mom`; 88 of 254 beat SPY sealed, median −4.5%, random controls −1.8 to −10.6%. | D | accepted — **the README's backtest section is rewritten from this** (dev / sealed / SPY / DSR / top-5 share / max DD per row, the random-control line, and the sentence "nothing passes the multiplicity bar"); the sealed top-10 + `skill_mom` get forward books (chunk E); the vectorbt replication runs on `top10_for_replication_2026-09-26.json`. |
| 7 | G: `/research` discards the quest's claims and sends "comments"; `/compare` measures direction (the wrong skill) on a six-number packet; the idle watchdog dies with its parent and the lab loop never calls `ensure()`; NVIDIA `nemotron-3-super-120b` is a rate-limited reasoning model whose empty answers get parsed from its reasoning. | reviewer G | accepted — G-fix builder: the 600-char evidence reply (holdings, rank decile, revisions, next catalyst, stop in σ, what changed, forecast id, falsifier); `/compare` on **extraction accuracy** (the bake-off E-G1: 240 dated news items graded against the revisions file, four arms, ~$0.15) with magnitude as the forward test; a separate reaper reading the owner note; a non-reasoning NVIDIA instruct model with 429 handling; errors redacted before Telegram. |
| 8 | H: fix 1 (cadence timestamps) is a correctness bug regardless; fix 2 (checkpointed hash chain) weakens tamper evidence for ~$0 — use `fadvise(DONTNEED)` after full reads instead; volumes grew 8.7 GB in five days because `counterfactual --record` appends every 10 minutes; retire hack2 (no positions since 09-04, heaviest downloads); the ssh check need not gate fix 1. | reviewer H+I | accepted as the recommendation to Murat for the terminal repo (not executed here: production is not mutated by a review). |
| 9 | B: 477 sources registered (456 brokerages from the revisions parquet, the corpus feeds, SEC), all n=0, weight = prior; a login wall is now its own status. F: twelve questions, triggers, claims → `claims.jsonl` + `web_events`, promises ledger with `promise:v1` rows; MU re-carded ($0.085). `web_events` lacks a `claim` type and a `source_id` field, so 20 of 26 claims live only in the claims ledger. | B, F | accepted — add `claim` + `source_id` to `web_events` (post-review fixes); restrict trigger (a) to personal/competition books (190 library names would eat the cap). |

## Wave 2 (three builders at a time; free memory ≈ 4 GB)
- **E** (bridge): freeze the sealed top-10 + `skill_mom` + the three PROBE-weighting twins as `$1M` books with twins; `bridge_report.py` (historical CAGR / sealed CAGR / max DD / expected turnover / forward return / forward SPY / relative / regime / days since inception, and the failure taxonomy); rewrite README "Historical backtests" from the leaderboard; the vectorbt replication of the replication file.
- **Post-review fixes**: learning report (direction/magnitude split in the sentences; the vol prior beside the LLM; EV list from measured spreads; `backend/tests` excluded from reachability); `web_events` `claim` + `source_id`; trigger (a) scope.
- **G-fix** (after one of the above finishes): item 7.
- **Reviewer A+B+F** now; **reviewer D+E** after E.
