# BUILD — B1 TRUTH — complete — 2026-09-04/05

**Session:** Opus 5 as builder, seven agents, per
`HANDOFF_2026-09-04_FABLE51_TO_OPUS5_BUILDER.md` §2.
**Blocks:** B1 (all six tasks) + B0v (a verification pass the roadmap did not
ask for) + two B2 items promoted on evidence.
**Nothing pushed. Nothing sealed. No order placed, modified or cancelled.**

---

## 1. RESULTS SCOREBOARD

| | |
|---|---|
| Best historical net strategy vs the market | **NONE. And now measured, not assumed.** On the rebuilt panel: BH-FDR band survivors **8 → 0**; the toxic short **+76.63%/yr → −29.25%/yr** (t_b −0.88) once reported as `−resid` on Reg-T capital; the S36 revision champion **TW 3.743 → 1.284** against an unchanged market TW of 3.41; and at 25 bps **no arm of 150** in the holding-period family has a positive excess CAGR (best −6.33pp/yr). Max |t| anywhere in that 268-cell family is **+1.533**, so no family correction could rescue it — the uncorrected p already fails. |
| Best forward paper strategy | Not accruing. All six books flat; both entry paths disarmed at the variable layer pending B2. hack4 is the only book with positive realised P&L (+$2,027) and $798 of that is churn. |
| Independent selectors live | **0.** Unchanged, and now for a measured reason rather than an inherited one. |
| Farm candidates tested / promoted | 15 / 0 (unchanged). |
| New actionable findings | **6** (§4). Two are P1. |
| External execution drag | Five PANW shorts, **−$725.35**, all **1:8 against** at seal time. |
| LLM spend this session | **$0.00.** No chat-provider call was made. |
| **RESULT IMPROVEMENT** | **NONE in return — and that is the result.** Every strategy number the program held was measured with a broken ruler on a corrupted tape. Both instruments are now fixed and pinned by tests, and the corrected numbers are worse across the board. The program's demonstrated edge did not fall from something to nothing; it was never measured. |

---

## 2. GATE B1 → B2/B4 — CHECKLIST WITH EVIDENCE

| gate item | status | evidence |
|---|---|---|
| benchmark module with tests | **PASS** | `learner/benchmark.py`; `backend/tests/test_benchmark_canonical.py` (21 tests). Reproduces the anchor **+96.67%** exactly |
| share-basis test green | **PASS** | `test_ibes_target_share_basis.py` — the `xfail(strict=True)` marker is deleted and it passes as a plain test |
| `dlret` merged + delisting-count receipt | **PASS** | 866 performance delistings (`dlstcd ∈ {500} ∪ [520,584]`), mean −24.63%; 53 Shumway fills; `panel_rebuild_20260904.json` |
| four receipts re-issued | **PASS** | `band_horizon_20260905` · `toxic_band_short_20260905` · `revision_6m_cohorts_20260905` · `holding_period_policy_20260905`, each with `supersedes:` and a `SUPERSEDED_BY` sidecar carrying **both** sha256s. Sealed originals unmodified (`git status` clean on all four) |
| INDEX updated | **PASS** | `docs/INDEX.md` TIER 1 |
| `FINDING_*_THE_TAPE_REBUILT.md` | **PASS** | `docs/FINDING_2026-09-04_THE_TAPE_REBUILT.md` |
| B1 §5 re-derive BAND_PRIOR | **CONDITION MET, ATTENDED** | no band premium survives a PIT ratio → `PROPOSAL_2026-09-04_BAND_PRIOR_TO_HYGIENE_ONLY.md`, three questions. `prior.py` untouched; every receipt declares `prior_status: VOID` |
| B1 §6 manifest | **PASS** | `docs/DATA_MANIFEST.md` + `test_data_manifest_complete.py` (parses the manifest for its prefix list) |

**Gate: PASS.** B2 and B4 are open. §5 is the only item awaiting a human, and it
is a decision rather than a build.

Suite: **6377 passed, 17 skipped, 118 deselected, 0 failed** (505 s) after the
tape receipts; the allocator change adds 7 more. The `xfail` is gone, so the
suite's `1 xfailed` line is gone with it.

---

## 3. WHAT WAS REFUTED, CONFIRMED, AND CANNOT DETERMINE

### Confirmed (re-derived independently, 11 numbers exact)
The share-basis defect (AAPL 19.323 vs 541.04, factor exactly 28.0); 74.35% of the
old toxic band carries a future reverse split vs 0.09% of `lt_1_5`; the
26,199-row re-bucketing census cell for cell; 866 delistings at −24.63%; the
market at +96.67%; the states persistent null at **p = 1.000** for k=3/4/5 with
the observed spread below **all 200** draws; `family_max_p` never called in the v2
driver and byte-identical to the single-arm null in v1; p = 0.0154 is exactly the
1/65 add-one floor.

### Refuted
- **Every band premium.** BH-FDR survivors 8 → 0. `lt_1_5` −2.17%/yr,
  `b_1_5_3` −6.82%, `b_3_5` −5.26%, and the toxic cell's +40.07% (t 1.97, 7
  names/mo) **flips to −34.29% (t −1.46) at close ≥ $5**.
- **The toxic short.** "Hedged gross" +76.63%/yr was a P&L per dollar of short
  notional on a 1.48× long market leg. On `−resid` over Reg-T capital:
  **−29.25%/yr**, with `beta_matched` legs of +18.78% and +34.89%/yr quoted
  beside it — i.e. the leverage was the return.
- **The revision contract's evidence base.** TW 3.743 → 1.284 on the same rule
  with only the admission column corrected.
- **Six of Fable 5.1's claims as stated**, three of them mis-attributions that
  would have misled the build. Full adjudication in
  `VERIFICATION_2026-09-04_OPUS5_ON_FABLE51.md`.
- **The `post_event_drift` mega-11 expression** (not the mechanism):
  **t 2.71 → t 1.45** raw simple, and PANW's DOWN-big cell **−0.852% t −0.59**.

### Confirmed-but-not-money — the session's most interesting single result
Over the **full PIT hygiene universe** (363,684 name-months, 5.4× the old pool),
**both** revision definitions beat **all 64** permutation draws from their own
pool — and **neither beats the market** (`target_rev_1m` TW 2.937, `net_rev_1m`
3.102, market 3.236).

**Ranking skill is real and is not money.** The two definitions agree, which is
the cross-check that makes it credible; the p = 1/65 is the add-one **floor**, a
censored bound, and the receipt says so rather than quoting it as a measurement.

### Cannot determine
| question | why | what settles it |
|---|---|---|
| Whether the unsupervised states result is real | two nulls bracket it: p = 0.000 random-partition, **p = 1.000** persistence-preserving | a null that controls the name path without destroying persistence — B4 |
| Family-adjusted inference over the learner's ~44 cells | **not computable** from stored draws: no per-arm null exists for 11 of 16 arms | B4 |
| Why hack2's protective stop was cancelled at 13:32:05Z on 09-04 | Railway log retention starts 15:32:53Z; `railway ssh` → "Host key verification failed" | the `exit` row at that timestamp in `/app/state/decisions.jsonl` on the hack2 volume |
| Any per-role fill history before 09-02 | fleet decision rows exist **only** on the Railway volume; the laptop holds 08-28 dry-runs | B3 — this blocked three separate questions in two days |

---

## 4. NEW FINDINGS THE ROADMAP DID NOT ANTICIPATE

1. **P1 — a guard scoped "outside the mega-11" left the only path that traded.**
   `FINDING_2026-09-05_THE_MEGA11_EXEMPTION.md`. The 2026-08-26 fix disarmed the
   wide universe and exempted the mega-11 by name; the mega-11 is the only path
   that ever placed one of these shorts. Nine days unguarded.
2. **P1 — five live shorts were 1:8 against before any signal question.**
   `expected_edge_usd` $92–128 against a stop worth $700+, both already in the
   book. A one-line comparison at seal time refuses all five.
3. **The fleet's ENTRY pass had no deadline gate** while the exit pass did, so the
   books churned post-expiry: hack2 opened a PANW short at 11:03:18 ET and the
   guard liquidated it at **11:03:42**. Fixed at the variable layer on all six
   (proven: 0 entry passes over a full 30-minute cycle, 7 exit passes each);
   structural gate in terminal `fd0c75b`, local, **needs a push**.
4. **`Mandate.tier` is declarative only.** Nothing outside `fleet.py` reads it;
   both SAFE books opened unbounded shorts by omission, in a structure that
   self-labels `UNBOUNDED` while sizing off `max_loss = spot × 0.05`.
5. **`exits.manage` never re-places a stop after a failed close** — hack2 was
   naked-short for 76 minutes. The code reasons about *cancel fails ⇒ don't
   close* and not the converse. **The bug saved $338** (the naked window
   contained the spike), which is exactly why it would never otherwise be found.
6. **14.25 GiB of duplicate WRDS parquet**, cause named:
   `wrds_pull_everything.py` keys resumability on the output **filename** and
   WRDS serves several libraries under two names, so each was pulled twice and
   neither run could see the other.

---

## 5. CLAIMS FOR FABLE TO ATTACK

| # | claim | receipt | what would kill it |
|---|---|---|---|
| 1 | No band premium survives a point-in-time ratio | `band_horizon_20260905` (104 cells, family-max p 0.808, min p 0.0517) | a band that clears a family-corrected bar with a $5 price floor and `dlret` merged |
| 2 | The toxic short is −29.25%/yr on Reg-T capital, not +76.63% gross | `toxic_band_short_20260905` (152 cells) | a costed short with real borrow that beats its own `beta_matched` leg |
| 3 | Revision ranking beats its own pool's 64 nulls and loses to the market | `revision_6m_cohorts_20260905` (85 cells, two definitions) | an expression that converts the ranking into money net of costs |
| 4 | No holding-period arm has positive excess at 25 bps | `holding_period_policy_20260905` (150 arms, 268-cell family, max t +1.533) | — the max t is the ceiling; this one is hard to kill and that is the point |
| 5 | The published market was +96.7%, not +740% | `signal_engine_backtest_20260904`; two independent rulers agreeing to 2.6pp | a dividend-inclusive tradeable series that gives +740% over the window |
| 6 | The signal engine keeps 24.7% of the market's return | same receipt (+28.3% net vs +114.8%) | note the caveat: the engine reads the series it is scored on, so this is a re-run not a re-scoring |
| 7 | The mega-11 PEAD edge is t 1.45 raw simple, not t 2.82 | `FINDING_2026-09-05_THE_MEGA11_EXEMPTION` §2 | a hedged expression keeping measurement and trade in one currency that still clears a family bar |
| 8 | 84% of round trips closing same-session is really 60% | independently re-derived; the 84% came from a 29-row hand-assembled file with two whole-basket rows | per-role venue history from the Railway volume |

Two claims I am *least* confident in and would attack first myself: **#3**, because
"beats all 64 draws" is a censored 1/65 floor over two non-independent pools; and
the corrected toxic cell's +40.07%, which I have argued at length should not be
carried forward at all (84% sub-$5, sign flips at a $5 floor, 2022-24 t 0.03,
27.6% still carrying future reverse splits).

---

## 6. DEFECTS THIS SESSION CREATED AND CAUGHT

Worth recording, because all three were caught by the discipline rather than by luck:

1. **The benchmark gate returned a false negative on its own first receipt.**
   `buy_hold_total_return` contains no market word. The tempting repair —
   widening `validate_stamp` to accept several producer modules — would have
   ended the gate; `declare()` keeps one producer and one validator and records
   `declared_only: True`. Two regression tests.
2. **`hpp.run_fixed` scored each pool from its own 24th rebalance**, so the two
   revision definitions came back against market TW **3.2362 vs 3.41** — and that
   difference would have read as a result. Fixed with an optional `start_day`;
   the receipt now records the invariant.
3. **A refusal-class regex matched on prose** (`past .* ET on judging day`) and
   would have retroactively re-labelled historical exit rows as entry refusals;
   it was also placed at `PATTERNS[0]`, displacing a class 473 rows depend on.
   Caught by the terminal suite, then narrowed to a literal token.

---

## 7. NEXT SESSION'S FIRST THREE TASKS

1. **B2 §1-3 — hold integrity**, with the two promoted items first: refuse when
   `expected_edge_usd < k × stop_loss_usd`, and make `tier` binding on side.
   Then hold fields on every sealed book, contract-aware typed exits, and a
   re-entry guard that sees every exit (and does not fail open).
2. **B4 — honest inference.** Three separate questions now sit blocked on it:
   the states null, the learner's family correction, and every family-max p in
   the four new receipts. `revision_6m_cohorts_20260905` is the ready-made test
   case — a ranking that beats its own pool and loses to the market is exactly
   what DSR/SPA are for.
3. **B3 — the loop speaks.** Not for its own sake: the fleet's decision rows
   exist only on the Railway volume, and that blocked four questions in this
   session alone.

## 8. ATTENDED ITEMS FOR MURAT (six, none urgent)

1. `PROPOSAL_2026-09-04_BAND_PRIOR_TO_HYGIENE_ONLY.md` — three questions; the
   live-threshold one matters most, because the fleet's band rule reads **Finnhub
   unadjusted** targets, a different object from the tape, with thresholds
   imported from the corrupted tape.
2. Push terminal `fd0c75b` (7 commits unpushed). The variables already hold the
   line, so this is belt-and-braces, not urgent.
3. 14.25 GiB of duplicate WRDS parquet — delete or keep. Forward fix is an alias
   table in the puller.
4. Restart the entry-timing tournament when B2 §1-3 land (paused by the
   `AAT_ENTRY_STYLE` deletion; it is the control arm for event-driven timing).
5. `aat-loop-staging` is Failed — "Deployment does not have an associated build".
6. `PROPOSAL_2026-09-05_DECISION_TIMING_NOT_POLLING.md` — your no-hard-rules
   principle is adopted and the cadence work is sequenced after B2 §1-3.

## 9. STATE AT HANDOFF

Finance repo: **19 modified, 14 new, local, not pushed.** Terminal repo: one
local commit `fd0c75b`, 7 unpushed; Railway variables changed on the six loop
services (`--manage-only` added, `AAT_ENTRY_STYLE` deleted on hack4/hack6) and
verified afterwards — services Online, exits alive, entries dead, ledger chains
intact, all six accounts flat. Optimus repo: untouched this session; run
`python tools/refresh_aegis.py` there so the brain sees this.

Session memory: five files written, `MEMORY.md` updated with four lines.
