# V5 Roadmap — Honest History (written 2026-07-18)

**Theme:** the forward track record (day ~40) is honest but young. V5 buys the
one thing money can buy here — survivorship-free history — and uses it for an
HONEST backtest-to-today of the exact lane mandates, third-party-verified,
so we can compare performance NOW instead of waiting 24 months. Plus the NN
trial that clean data finally makes legitimate.

## Non-negotiables (unchanged from CANON)

1. **The backtest is a direction-check, not the track record.** Historical
   replay results are displayed on a SEPARATE methodology/history view and
   written to research docs — **never into `paper_nav`** (CANON §5, the
   write-path is sacred). A good backtest number is never reported as "we
   beat the market."
2. **Purged CV + DSR/PBO deflation** on anything where a parameter was
   chosen. Even on clean data, picking the best of N variants is overfitting.
3. **Six forward clocks is the cap.** Everything in V5 is offline research
   until a trial is explicitly pre-registered (attended, Murat-approved).
4. **EODHD is offline-validation-only.** The token stays local / research
   scripts; it does not become a live serving dependency on Railway.
5. Every gate has a pre-committed pass bar written BEFORE the data is seen.

## Gate results so far

| Gate | Bar | Result | Verdict |
|---|---|---|---|
| EODHD Phase 1 (free key, delisted list) | ≥16/20 audit names listed | **16/20** (2026-07-18, `engine/research/eodhd_acceptance_2026-07-18.json`) | **PROCEED — subscribe All World** |
| EODHD Phase 2 (paid key, usable history) | ≥16/20 usable (≥250 rows, ends at death year ±1) | pending purchase | if FAIL → cancel, quote Sharadar SEP |
| Alpaca paper keys | account ACTIVE | ✅ 2026-07-18, $100k, 0 positions | ready for attended seed |

## Chunks

### Chunk 0 — Murat, today (~15 min, unblocks everything)
1. **Buy EODHD "All World" ($19.99/mo)** at eodhd.com → Pricing. The Phase 1
   gate passed; the purchase is justified. Use the SAME account as the free
   key (the token stays the same; the plan upgrade unlocks full history).
2. **Railway FMP key is still the OLD leaked one** (`caDsM…`) — the local
   `.env` has the new rotated key. Update Railway `FMP_API_KEY` to the new
   value and redeploy, otherwise prod is still running on the compromised key.
3. **Alpaca seed** (attended, per seed-a-lane): Railway → set
   `AEGIS_SEED_ALPACA_MIRROR=1` → deploy once → confirm seed in logs /
   `/api/health/full` → set it back to `0`. The seed is idempotent; a
   seeded account never re-seeds.
4. Rotate the keys that passed through chat when convenient (Alpaca paper +
   DeepSeek + Finnhub; low blast radius but hygiene is hygiene).

### Chunk 1 — Phase 2 acceptance (first session after purchase, ~10 min)
`python -m engine.research.eodhd_acceptance --phase 2` with the paid token.
Pass bar ≥16/20 usable, frozen 2026-07-16. **If it fails, cancel inside the
billing period and pivot to Sharadar** — do not build a loader on data that
failed its own acceptance test.

### Chunk 2 — EODHD loader + local panel (1 session)
`engine/research/eodhd_loader.py`: EOD adjusted prices incl. delisted names,
cached to parquet under `engine/data/eodhd/` (gitignored). Universe
construction is the hard honest part: point-in-time membership (delisted-
inclusive), documented rule, no hand-picked survivors. Cross-check 10
overlapping tickers against yfinance closes (tolerance gate) before trusting.

### Chunk 3 — Honest historical replay, 2015 → today (1-2 sessions)
Replay the EXACT lane mandates (same YAMLs, same rebalance rules) on the
survivorship-free panel vs SPY. Report CAGR, maxDD, Sharpe **with DSR** and
PBO across any tried variants. Output: `docs/research/HONEST_REPLAY_*.md` +
a **separate** "Historical replay (not the live record)" page/section in the
UI, clearly labeled, linking to methodology. This answers Murat's "compare
our performance to other models now" — honestly.

### Chunk 4 — Third-party verification (1 session)
Mirror the same mandates on **QuantConnect** (free cloud, survivorship-free
since 1998) → a shareable third-party backtest URL nobody can accuse us of
fabricating. Pairs with the Alpaca mirror (third-party FORWARD NAV): both
halves of the record verified by someone else's servers.

### Chunk 5 — Neural network, done right (gated on Chunks 1-3)
The long-promised NN/CNN becomes legitimate once the clean panel exists.
Offline first: pre-register the protocol (architecture class, features,
purged-CV walk-forward, DSR bar) via `pre-register-trial` BEFORE training.
It only becomes a 7th forward clock if Murat explicitly lifts the cap.

### Chunk 6 — Housekeeping (background)
- Confirm the yfinance 401/throttle storm eases with EODHD as the offline
  cross-check source; keep prod's provider chain unchanged.
- Chen–Zimmermann open-source predictor set: shortlist 3-5 (not 212) as
  CANDIDATES for future forward-IC slots — evaluation offline only, cap
  respected.

## What V5 does NOT do
- Write any backtest number into the forward track record.
- Add EODHD to the live serving path.
- Register new forward trials without the attended pre-registration flow.
- Claim skill, beat-the-market, or alpha from any replay result.
