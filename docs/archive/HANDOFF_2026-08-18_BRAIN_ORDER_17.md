# ORDER — brain → builder (order 17) — IIF becomes a bounded ops track; the research engine starts today

Binding. Adjudicates GPT round 2 against Orders 15/16 and the canon; where they
conflict, this order wins. Murat's ruling is adopted as the frame: **the
operational IIF work stops determining the development order of the research
engine.** Night 2 is one frozen experiment consuming a bounded slice of one
machine's evening; everything else runs continuously.

## 0. Answers Opus is waiting on

1. **Push — go.** Three commits (`e221e31`, `a921007`, plus Order docs). Verify
   the commit flip; Railway should SKIP; the live verification target is the
   monitor workflow's next run.
2. **The scheduled task is ALREADY REGISTERED — do not register it twice.**
   The brain session ran the printed `schtasks /Create` at ~14:3x today;
   `SUCCESS`, unarmed. `--acceptance` already shows today's manual rehearsal
   correctly excluded. First scheduled firing is tonight 17:00 →
   `REFUSED / NOT_ARMED` receipt #1.
3. **`iif1_frozen` worktree — go.** Manifest detects drift, worktree prevents
   it; both, as Order 16 adopted.
4. **Night 2 runs attended tonight at 17:00** — Murat has confirmed he will
   launch. `--dry-run` immediately before; the boundary is recomputed, never
   quoted.

## 1. Two concessions, on the record

- **"~95 years" is a claim-specific power calculation, not doctrine.** §59's
  number was computed for +3%/yr net on a co-moving equity slice with a
  terminal-return outcome. Every new claim recomputes its own requirement from
  its own effect size, dependence and estimator — which is §64 run forwards,
  and is what the daemon's power screen does per job. The *direction* stands:
  risk and relative outcomes resolve far faster on identical data.
- **The arena's k_eff is MEASURED, not asserted.** "≈1–2" was a directional
  warning. Canon already forbids a third branch: ρ̄ is `MEASURED` or
  `DECLARED_CONSERVATIVE`. When arena books exist, their effective
  dimensionality is computed from action overlap, holdings overlap and
  return correlation with shared resamples — and printed next to any
  cross-book comparison.

## 2. The G5 ruling, refined not reversed

GPT argues NEURAL-RELATIVE-VALUE-1 differs from G5 because the primitive unit
is **relative capital allocation between two securities** using state G5 never
saw. That *is* a statable distinct claim — which is exactly what Order 16
demanded. The rule stands unchanged: **the registration names G5's three
receipts and states the distinct claim in one sentence**; the sentence now
exists. Same for MODEL-DISAGREEMENT-1: "disagreement topology is information"
must state what it asks that G5's conditional-shape tests did not.

## 3. The updated queue

### Track F — frozen forward lane (bounded; never sets the day's agenda)
- Tonight: attended Night 2 (Murat). Launcher acceptance accrues passively;
  arm `AEGIS_IIF1_LAUNCHER_ARMED=1` after 3/3, likely Friday.
- Bounded reliability jobs, timeboxed: early-fire-and-hold launcher trigger
  (the 2.9-min jitter margin) · **the minimum meaningful paired Brier
  difference, drafted for Murat's signature BEFORE 08-21** · write-only
  telemetry audit · 08-21 attended resolve run (Murat).

### Track R — the research engine (the main bandwidth, starting now)

**P0-R1 · AEGIS-RESEARCH-DAEMON-1** — subsumes RESEARCH-FACTORY-1 and
**includes HYPOTHESIS-BANDIT-1 from day one** (priority = expected value ×
P(resolves) × information gain ÷ cost, fixed before the result — mission rule
5 operationalized; retrofitting scoring onto a FIFO queue later is a rewrite).
Every job carries hypothesis ID, parent/corpse ID, data hash, code SHA, PIT
cutoff, MDE/power, cost model, seeds, output hash. Hard constraints wired in,
not policed after: routes through the single `decide()` (§63, BH-FDR m =
the daemon's own running test count); corpse dedup via `distinct_from`; §64
power screen before queueing; **the reserved confirmation windows are
unreachable by construction** — a daemon job whose data range touches them is
refused at submission, not at analysis. Yields resources around the launch
window. Its nightly receipt is the dashboard GPT sketched: episodes screened /
clusters found / corpse-matched / underpowered / screened out / promoted /
survived / confirmed / features queued / utilization. **The PC wakes up with
work to do.**

**P0-R2 · AGK + convention audit** — unchanged from Order 16, and it gates
more than before: NEURAL-RELATIVE-VALUE-1's labels are *net-of-cost* relative
outcomes, so the pairwise model cannot be labeled until the cost model and its
half-vs-round-trip convention are frozen.

**P0-R3 · AEGIS-NET-DATASET-1** — canonical PIT panel + labels for all heads
(rank, quantiles, magnitude, drawdown, continuation/reversal, first-passage
barriers). Starts today, in parallel with Track F, per Murat's ruling.

**P1 (in order):**
- **AEGIS-NET-TOURNAMENT-1** — linear / LGBM / NN1 / NN2 / NN3, same
  data/folds/costs/labels, feature-family ablations, no security-ID
  embedding. Pre-registration draft → Murat signs → run. Order 16's rules
  ride along: shared resamples, k_eff printed, §37 on the winner, barrier
  heads evaluated on the forward horizon only.
- **NEURAL-RELATIVE-VALUE-1** — pairwise incumbent-vs-candidate; compare
  pairwise logistic, LambdaMART/LGBM ranking, shared-weight MLP. G5 named
  (§2). One addition GPT did not state: **pairs explode the row count, not
  the information** — n_effective counts DATE BLOCKS (§58); a million pairs
  on 500 dates is 500.
- **LANE-AUTOPSY-1++** — the counterfactual replay design (one difference at
  a time: weights, sells, trims, stops, cadence, cash) replaces the
  Brinson-only version. The 14-point mirror-vs-conviction gap is the most
  information-dense number on the board and it is dissected NOW, descriptively
  — the 2027 decision date gates the *verdict*, not the *diagnosis*.
- **CONVEXITY-PRESERVATION-1** — the +20/+40/+75/+100 episode library with
  action arms (hold / trim / stop / exit / re-underwrite / re-enter). The
  trailing stop is tested as an ARM, which is the correct home for §15.
- **EVENT-RESOLUTION-CURVE-1** — §62 turned into an instrument: per event
  class, the fraction of the move in each window after the point Aegis could
  have known and acted. Prior: G4's 87–95%-while-shut. This number then
  gates every event-strategy design downstream.
- **INFORMATION-HALF-LIFE-1** — edge as a function of information age per
  delayed source (PTRs, 13D/G, Form 4, revisions, filings, news), after
  realistic ingestion latency. This is rule 5 pointed at our own data
  spending: it decides which ingestion systems are worth building.

**P2:** SURPRISE-OF-SURPRISE-1 (Order 16's expectation-dislocation amendments
carry: §62, `distinct_from` the dead target-levels) · FAILURE-TO-REACT-1
(screen-grade; overlaps dislocation — the daemon's dedup decides) ·
MODEL-DISAGREEMENT-1 (G5 sentence required) · CROSS-ENTITY-LAG-1 (simple 1/2-hop
propagation ONLY after the PIT edge source is registered; the GNN is earned,
not assumed) · **BLOOMBERG-CHALLENGE-SIM-1** — a sim harness on the 2025 HKU
format ($1M, five weeks) as a concentrated-allocation stress test; NOT a
registered lane, NOT an evidence stream, provisional parameters replaced when
Bloomberg publishes 2026 rules. The 2026 window is unverified as of today —
nobody invents a date; Murat checks HKU Business School when term starts.

## 4. Standing

- **Bounded ops do not set the agenda.** A frozen experiment consumes its
  slot, not the day.
- **A distinct claim is a sentence, not a feeling** — G5 (and any corpse) is
  confronted by name, and the sentence goes in the registration.
- **Compute pays for breadth and robustness, never for evidence by
  repetition** — bootstrap tells you about your estimator, not about the
  world; both are worth knowing and only one is alpha evidence.
- **Power requirements are recomputed per claim.** No number from one claim's
  power calculation becomes another claim's doctrine.
- **The daemon's throughput is its multiplicity.** m = tests run, counted by
  the machine that runs them, and reserved windows are unreachable at
  submission time.

— brain, 2026-08-18
