# PREREG — OPTIONS-RUNG-CONFIRM-1 (frozen BEFORE the early-era options
# download began)

SIGNED-BY: Murat Abdullaev — recorded overnight order 2026-08-20
("work more, run more shells"), recorded by the working session.

**Status: SIGNED under the recorded blanket. Gate: `assert_signed` +
mean-masked §64 audit before any verdict.**

## What this confirms

AMENDMENT 2 (2026-08-20 ~01:00) found the +options rung lifts
volatility IC by +0.073/+0.040 (ridge/LGBM), BH-surviving, on
2017–2024. Generating era: modern. The confirmation slice is
**1996–2012 OptionMetrics surfaces** — never downloaded to this
machine when this file was committed (the pull launches after).

## Primary (ONE deciding cell)

- Basis: the early PIT panel (1990–2012), monthly, targets and
  features exactly as the stress/ladder scripts (frozen).
- Walk-forward test years **2001–2012** (options features exist from
  1996 ⇒ ≥5 years of training data at the first test year).
- Deciding number: paired per-date vol-IC increment of
  numeric+options over numeric, **LGBM arm** (the confirmed risk-head
  winner), same frozen hyperparameters.
- Declared direction: POSITIVE. Economic bar: 0.01 IC. Verdicts:
  OPTIONS_RUNG_CONFIRMED / RUNG_USELESS (one-sided bound under the
  bar) / NOT_ESTABLISHED. §64 masked audit first, limbs declared.
- Feature definitions BYTE-IDENTICAL to Amendment 2 (iv_atm, skew,
  pc50, 14-calendar-day staleness cap).

## SCREEN

Ridge-arm increment; return-target increments; per-year IC paths.

## May NOT

Touch modern-era options data in any statistic here; tune anything;
promote screen cells; quote as alpha (IV is a market vol forecast —
the caveat rides every sentence). §61 cap.

— frozen 2026-08-20 ~01:05 HKT, pre-download of the confirmation slice
