# Amendment — 2026-09-25 afternoon — Murat's build orders (books, forecasts, the daily review)

Amends `HANDOFF_2026-09-25_FABLE_TO_OPUS_BUILD_PLAN.md`. Same global constraints.
Code facts verified this afternoon: `research_notes/2026-09-25/code_facts.md`.
Four Opus builders run in parallel on **disjoint files**; Fable validates.

## Murat's orders (his words, condensed, 2026-09-25 afternoon)
1. "continue making forecasts and decisions everyday with every nightly sim ... so we can review them later." → **every sim day produces forecast rows and a decision receipt; zero is red.**
2. Personal book: **6-month return**, "engine + basic human intervention", **no 10% cap on a name if it is good**; reconsider QUBT and DKNG (he holds both, `murat_book.yaml`); use social reads (Reddit/X) to find additional names; consider his holdings "for consideration, not a guide"; add pharma.
3. "till the competition begins create 10 paper accounts ... only update them once a day or once the PC opens, make it cheap, we can just register the names and see how they moved on a daily check." → **ten personal books, frozen, graded once a day.**
4. "for Bloomberg make 10 paper accounts like that using the current news, analyst numbers, tech focus and demand and the news, politics, geopolitics, also it should cover global stocks not focused on Nasdaq" ... "max 10% ... aim for grand prize 50% return" → **ten competition books, global, ≤10%/name, objective Relative P&L vs WLS, Oct 12–Nov 13.**
5. "create this list with help of LLM + news + engine ... use openclaw and deepseek for social media and news digestion" ... "include analyst returns and weight that too because ROI is the main goal" → **the book generator reads the briefing, the news corpus, the revision flow, the catalyst calendar and the research notes; DeepSeek drafts, paired with the local model; every book carries a strategy paragraph and per-position falsifiers.**
6. "run a backtest on this strat too because we got that data and we have the decision vs what happened" → **revision-flow sweep (C2) + a decision autopsy over `decisions/*.json`.**
7. "monitor the stock account before market once per day or once it hits the trigger but don't sell immediately when it jumps or drops, see what might happen and based on that make a decision" → **a daily pre-open review of every held name with hold/trim/sell/buy_more and a forecast row; a >2σ move sets WATCH for one session before any sell.**
8. Void the six unpriceable forecasts: **no** (his decision).

## Builder O1 — the sim makes forecasts and reviews holdings every day
**Owns:** `scripts/sim_run.py`, `backend/services/sim_session.py`, `backend/services/accrual_canary.py` (new), `backend/services/daily_review.py` (new), `scripts/night_investigator_forecast.py`, `backend/services/openclaw_client.py`, the health composer row, `backend/tests/test_accrual_canary.py`, `backend/tests/test_daily_review.py`, `backend/tests/test_u_forecast.py`.
1. **C0** exactly as in the build plan (accrual canary; OpenClaw telemetry row incl. `EMPTY_LOG`; session row written at START; `n_considered` stuck detector beside the funnel's `n_candidates`).
2. **`u_forecast`** (new unit, after `rank`, before `plan`): once per UTC day (idempotent via `<OPTIMUS_LEDGER_DIR>/forecasts/day_<date>.json`), run the investigator process (`night_investigator_forecast.evidence_packets` + `ask`) on the union of: the funnel shortlist, `murat_book.yaml` tickers, every ticker in every frozen `llm_portfolio` book (read `books.jsonl`), and the top-N revision-activity names; **h=1 and h=5** rows each; `--model deepseek/deepseek-flash`; cost per call written to the llm telemetry (`purpose="u_forecast"`); hard caps `config.FORECAST_MAX_NAMES_PER_DAY = 60`, `config.FORECAST_DAILY_CAP_USD = 2.0` read from the **same ledger the writer writes**. A day with zero rows written is `DEGRADED` in the unit result. Retire the nine thematic persona arms from whatever cadence job still lists them (grep `specialist` in `backend/services/` and `scripts/`), recording `RETIRED_WEIGHT_ZERO`.
3. **`u_review`** (new unit, first cycle after 08:00 ET and again when a held name moves >2σ_63 intraday): `daily_review.review_holdings(asof, *, bars, books)` over (a) `murat_book.yaml` positions, (b) PC-PAPER positions (`pc_broker.snapshot`), (c) every frozen llm_portfolio book's positions. For each: last close, 1d/5d move in **sigma**, the latest forecast row (h=1/h=5) if one exists, the next dated catalyst from the YAML, and a label via `agency.decide_label(p, weight=w, max_single_name=...)` **wrapped by the patience rule**: if |1d move| > 2σ and no prior WATCH, label = `WATCH` (reason: "moved 2.4σ today; decide next session"), never `sell`; the next session decides. Writes `<OPTIMUS_LEDGER_DIR>/review/review_<date>.json` and a 15-line `morning_<date>.md` (holdings table + labels + reasons). Each label is also a forecast row (`specialist="review:v0"`, observable = the label's implied direction, h=5) so labels get graded.
4. Tests: canary (given in the plan); `u_forecast` writes rows only once per day and refuses over cap; `u_review` returns WATCH not SELL on a 3σ drop, and SELL/TRIM is allowed the following session; a held ticker absent from the panel is reported `UNPRICED`, not skipped.

## Builder O2 — the book factory (ten personal + ten competition books, graded daily)
**Owns:** `backend/services/llm_portfolio.py`, `scripts/llm_portfolio.py`, `scripts/book_factory.py` (new), `backend/services/global_prices.py` (new), `backend/data/optimus/pm_catalysts/catalysts_2026-Q4.yaml` (new), `backend/tests/test_llm_portfolio_twins.py`, `backend/tests/test_book_factory.py`, `backend/tests/test_global_prices.py`, `config.py` additions `BOOK_*`, `THEME_ETF_MAP`.
1. `llm_portfolio.freeze` gains `kind: "personal"|"competition"` and `constraints`: competition = long only, `max_weight ≤ 0.10`, no ETFs, cash ≤ 0.02, `objective` must name "Relative P&L vs WLS"; personal = long only, `max_weight` free, cash declared. Freeze **refuses** an unpriceable ticker unless the ticker resolves through `global_prices` (below).
2. `global_prices.ensure(tickers, *, start) -> pd.DataFrame` — yfinance daily closes for non-US suffixes (`.TW .KS .T .HK .AS .PA .DE .L .ST .OL .MI .TO .AX .SW .KQ .SS .SZ .NS`), cached append-only at `<OPTIMUS_LEDGER_DIR>/llm_portfolio/global_bars.parquet`, one pull per UTC day, receipt with `n_ok/n_failed`; `grade` unions it with the US panel; the WLS proxy = `URTH` (record it as a **proxy** and print its tracking caveat) until Murat supplies the WLS series from the Terminal.
3. `llm_portfolio.twins(book, *, asof, seed) -> dict` = `ew`, `sector_etf` (`THEME_ETF_MAP`), `spy` (or `urth` for competition), and `random_same_band` (same count, same liquidity band, `default_rng(seed)`); each frozen with `parent_book_id`.
4. `scripts/book_factory.py generate --kind {personal|competition} --n 10 --asof today --out <dir>`: assembles one **evidence pack** per candidate name (briefing row; 90-day revision flow computed directly from `analyst/target_revisions.parquet`: net raises, n_firms, median target change; news corpus last 14 days: headline count + top 5 titles by source; catalyst YAML entries; and the research notes' candidate tables passed as `--notes docs/research_notes/2026-09-25/research_themes.md research_global_candidates.md research_pharma.md research_social.md`), then asks DeepSeek (`deepseek-chat` via `llm_analyzer`, language pin inherited) for N books with **different declared strategies** (e.g. AI-power global, catalyst calendar, revision-flow leaders, policy/geopolitics, retail-attention contrarian, pharma binary basket, quality-momentum, small-cap catalyst, Asia supply chain, ensemble) — one call per book, the same prompt also to the local model (`llama-server` if up; skip with a receipt line if not), both answers saved; DeepSeek's is frozen. Every position carries `thesis` + `falsifier`; every book carries `strategy` and `what_i_did_not_buy`. Cost per book logged; cap `config.BOOK_FACTORY_CAP_USD = 1.0` per run.
5. `scripts/llm_portfolio.py grade` becomes the **daily check**: grades every book and every twin, writes `leaderboard_<date>.json` (NAV, vs benchmark, vs `ew`, vs `sector_etf`, vs `random_same_band`, by book kind) and prints a 20-line table. Idempotent per day. Builder O1's `u_grade` will call it; until then a scheduled task can.
6. Catalyst YAML: the 11 primary-verified PDUFA dates from `research_fda_catalysts.md` (CYTK struck) + the newly verified ones in `research_pharma.md` (SVRA, BBIO, COGT ×2, SMMT, INSM, BLTE, MRK Oct 10) + confirmed earnings dates in `research_themes.md` / `research_global_candidates.md` marked `[CONFIRMED]`. Each row: `{date, ticker, kind, what, source_url, verified_from: primary|aggregator}`.
7. Do **not** run `generate` for real from the builder (it spends); wire, test with a stubbed LLM, and leave the exact command in the commit message. Fable runs it.

## Builder O3 — revision flow, the sweep (the backtest), and the decision autopsy
**Owns:** `backend/services/revision_flow.py` (new), `scripts/revision_flow_sweep.py` (new), `scripts/decision_autopsy.py` (new), `backend/tests/test_revision_flow.py`, `backend/tests/test_decision_autopsy.py`.
1. `revision_flow.compute(revisions, *, asof, window_days=90)` exactly as chunk C2 (tests given there), reading the parquet, PIT-strict, refusing `pit_safe=False`.
2. `scripts/revision_flow_sweep.py`: the C2 sweep with and without the flow columns as extra features to `xs_ranker.walk_forward`; horizons 5/21/63; k 20/50/100; prints **first** by-year, leave-one-year-out, worst breadth cell, small vs largemid; NW blocking derived from the horizon; receipt `analyst/revision_flow_sweep_<date>.json`. Additionally a **simple long-only rule backtest** Murat asked for: each month-end, buy the top-k by `net_raises × n_firms` among names with ≥3 firms, hold 21/63 days, net of `round_trip_bps`; print by year and vs SPY.
3. Freeze two llm_portfolio books at the end: `revision_flow_v0` (top-20 by the rule, equal weight, kind personal) and `revision_flow_v0_random_twin` (same band, random, seed 20260925) via the existing `freeze` — O2's constraints may not exist yet; use the current API.
4. `scripts/decision_autopsy.py`: for every `decisions/YYYY-MM-DD.json` row with a ticker and a direction (BUY/PROBE/REFUSED), the realised 1/5/21-day relative return vs SPY from the bars; a table by direction and by day; "refused names that went up" and "bought names that went down" lists; receipt `decisions/autopsy_<date>.json`. Same for `predictions.jsonl` rows made since 09-11 (the 40 + whatever O1 adds), grouped by specialist.

## Builder O4 — the reputation layer
**Owns:** `backend/services/forecast_reputation.py` (new), `backend/tests/test_forecast_reputation.py`, `scripts/night_specialist_scoreboard.py` (add `new_rows_since_last_run` and the weights table).
Exactly chunk R of the build plan (tests given there): `arm_skill` held out by date, `weights` with shrink/floor/γ, `pool` log-odds + κ, `calibration_curve` by year for `investigator:*` at h=1 and h=5, the Profit-Mirage restricted-sample check, receipt `reputation/reputation_<date>.json`. No sim wiring (O1 owns `sim_run`); expose `refit(ledger_path) -> dict` for O1 to call later.

## Rules for all four
- Commit only your own files (`git add <paths>`); never `git add -A`; never push; never `reset/checkout/stash/clean`; `predictions.jsonl` is tracked and grows.
- Run your own tests, then the fast suite once at the end: `AEGIS_PERSONAL_MODE=0 AEGIS_IGNORE_DOTENV=1 python -m pytest backend/tests/ -m "not slow" -x -q`. Report the exit code; a red suite is a red report, not a footnote.
- New services modules get a caller or a `signal_reachability` classification; new exceptions enrol in `test_guard_missing_input_contract.CASES`.
- No live LLM spend from a builder except a single smoke call where a prompt must be proven to parse (say so in the report with its cost).
- Write a 30-line report at the end: what was built, the exact commands Fable should run, what is untested.

## Builder O5 — thesis cards: one structured analysis per chosen stock (added 2026-09-25 evening)
Murat: *"with all the stocks chosen run a stock analysis, run openclaw for doing web scraping about it, get its analyst reviews and news and upcoming dates, market strategy, demand, all the review."*

**Owns:** `backend/services/thesis_card.py` (new), `scripts/thesis_cards.py` (new), `backend/tests/test_thesis_card.py`. Imports (read-only) `openclaw_client.agent`, `llm_analyzer`, `xs_ranker`, `llm_portfolio.build_briefing` (or its row builder), the revisions parquet, the news corpus, the catalyst YAML.

**Universe, in priority order** (a `--universe` arg takes a file of tickers; defaults to this order): (1) `backend/data/murat_book.yaml` positions; (2) `docs/research_notes/2026-09-25/book_human_ai_thematic_v0.draft.json` positions; (3) the 17 names in `research_pharma.md` § Suggested candidate list; (4) the three global books in `research_global_candidates.md` (non-US tickers carry their Bloomberg/Yahoo suffix; OpenClaw reads the company's IR page in English where it exists). Resumable: a ticker with a card dated today is skipped.

**Card = engine side + web side + synthesis**, written to `<OPTIMUS_LEDGER_DIR>/thesis_cards/<date>/<TICKER>.json`, FLAT keys (the LLM's JSON must be flat; nested objects broke 55% of parses on 09-24):
```
ticker, name, asof, kind (personal|competition|holding),
engine_px, engine_mom_21, engine_mom_63, engine_vol_63, engine_band, engine_cost_bps,
engine_rev_qoq, engine_gross_margin, engine_gross_margin_chg, engine_inflection_flag,
engine_net_raises_90d, engine_n_firms_90d, engine_median_target_change_90d,
engine_forecast_p_up_1d, engine_forecast_p_up_5d (latest investigator rows if any),
news_30d_count, news_30d_top (list of "date | source | title | url", ≤8),
analyst_actions (list of "date | firm | action | from→to", ≤10, from OpenClaw + the parquet),
consensus_n, consensus_mean_target, consensus_implied_upside, consensus_source,
upcoming_dates (list of "date | kind | what | source_url | primary|aggregator"),
product, demand, pricing_power, competition, team, market_strategy,
bull, bear, falsifier, verdict (supports|neutral|against), confidence (low|med|high),
sources (list of urls), openclaw_log_path, openclaw_elapsed_s, deepseek_cost_usd, card_hash
```
**Web side** = ONE `openclaw agent` quest per ticker (model `deepseek/deepseek-flash` unless `--model`), prompt = the ten fixed questions (what changed / not known yesterday / demand vs price vs volume vs mix / suppliers / customers / who loses / contradicting evidence / if management right / if wrong / 2nd-3rd order beneficiaries) + "list every analyst action in the last 90 days with firm and date" + "list every dated upcoming event from the IR page" + "return FLAT JSON with these keys"; allowed sources per `analyst_ledger` source policy (IR, SEC, exchange notices, major wires; blogs context-only). An EMPTY log = a refusal card (`verdict: "REFUSED_EMPTY_LOG"`), never a silent skip. **Synthesis** = one DeepSeek call (`llm_analyzer`) that merges engine + web into `bull/bear/falsifier/verdict`, told the standing findings (§17 target level perverse; §59; §63 no dose-response; §64) so it does not re-derive them.

**Caps**: `--max-quests N` (default 40), `--cap-usd` (default 5.0, read from the same telemetry ledger the writer writes), `--parallel 2`. A digest `<date>/DIGEST.md` (one line per card: ticker | verdict | confidence | next date | falsifier) is rewritten after every card. Cost and elapsed printed per ticker.

**Seed run (Sonnet, today, before O5 finishes)**: the same schema, produced by hand for the first 12 tickers, into the same folder, so O5's resume logic skips them. Any schema drift between the seed cards and O5's is O5's bug to reconcile (it reads the seed cards and validates them).
