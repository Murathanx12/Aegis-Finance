# BUILD — CONTINUATION 2026-09-06 (Opus 5 coordinating six agents)

Mandate `docs/CONTINUATION_2026-09-06_OPUS_PROMPT.md`. **Nothing pushed,
sealed, ordered, deployed or changed on Railway.** Every number has a receipt
under `backend/data/optimus/continuation_2026-09-06/`. Long form, with the
tables and the mechanisms: **`BUILD_CONTINUATION_2026-09-06_DETAIL.md`**.

## RESULTS SCOREBOARD

**RESULT IMPROVEMENT: NONE.** Nothing reached NOVEL. Three mechanisms were
bought, built and measured; **none beat what we already had.**

| KPI | This session |
|---|---|
| Best historical net strategy vs the market | unchanged. `target_rev_1m__xs` top-50 VW: TW **85.324** vs VW market 13.031 (t 2.407) — but **+4.01%/yr, t 0.859** vs an EW market, and **DSR 0.1239** over its own 307-trial search |
| Best forward paper strategy | none. Books flat by design; hack3/4/6 dry-run only |
| Independent selector count | **unchanged at 1** |
| Farm candidates tested / promoted | **3 / 0** — companyworld graph features, floored neural encoder, LLM era-replay decider |
| New actionable finding | **the mandated baseline is five months.** 83.6% of `lgbm_clf`'s 251-month excess is five months; without them it is **behind the market** (7.78 vs 8.18) |
| External execution drag | not measured — no orders, no seals, no deploys |
| LLM spend / cost per gradeable output | **$4.14 of a $15.00 cap.** $0.00105 per supply-chain edge, $0.00063 per graded LLM rank decision |

**Fast suite: 6,627 passed, 14 skipped, 119 deselected.** Terminal repo:
**76 suites, 3,483 checks, ALL PASS** (from 74 / 3,367).
**20 local commits** here, **4** in the terminal repo.

## THE WORK, BY ITEM

| § | verdict | the number that decides it |
|---|---|---|
| **0 CUDA** | **a different interpreter, not a downgrade** | the venv's `site-packages` directory mtime has not moved since 2026-08-27, so no torch was added or removed on 09-05; the CUDA wheel went into system Python 3.12 at **11:49**, 101 min before W3 ran. The GPU numbers were always real; the receipt just never named the interpreter. Fixed + pinned (`requirements-gpu.txt`, 4 tests, FAIL on this laptop / SKIP on CI) |
| **2a supply-chain** | **CANNOT DETERMINE — and the direction closes the scope excuse** | 2,020 edges / 945 permnos / 1999-2011 for **$2.12**, 94.65% quote-verified, every PIT invariant clean. Customer momentum **FM t 1.447 → 0.297** on the never-seen tape (pooled 0.989). PBO 0.96 / 0.34 / 0.51. **More tape made it weaker** |
| **2b era replay** | **NOISE — all four arms negative** | 192 windows of 2016-19, rank graded only vs the EW basket of the same 8 anonymised names. Family-max p **0.729**, nothing survives BH-FDR, DSR 0.081, SPA p 1.00. **Blind held: 0 of 768 decisions named the true year.** MDE 8.75%/yr, so this is *not detectable on four years*, not *absent* |
| **2c neural** | **B10 NOT EARNED — the loop is stopped** | rule declared and hashed **before** the first fit, judged on the seed-mean ensemble. Floor on the TRAINING universe (530,447 of 925,757 rows; the grading floor then removed 0 of 454,708). `nn_pre_causal` clears 3 of 4 clauses, fails the family: **DSR 0.1726** vs a 0.95 bar. Best cell (TW 105.0) explicitly not promoted |
| **2d Monday** | **built, dry-run, and one decision surfaced** | **hygiene-only, implemented literally, EMPTIES hack6** (15→0) and halves hack3 with an entirely different five — the band's return constants were the SOURCE of `exp_return`, so 799 of 810 fail the coherence floor against 35 of 806 today. Hygiene itself excludes 33 and its `fails_only` is **zero**. Flag OFF by default; live fleet byte-identical |
| **2e registry** | **done** | 12 `conditional_evidence` rows, 3 families × 4 eras; `weekend-W7-matched-loser` excluded as SUPERSEDED **and the count named** — 9 of the memory's 12 SUPPORTED cells are in it. Superseded-cannot-vote **proved red** (5 of 11 tests fail on the reverted code) |
| **2f contract** | **drafted, not frozen** | `CONTRACT_DRAFT_2026-09-06_REVISION_BOOK.md`. Power line: ~36 years needed for t=2, 25.7 on hand; **the book tests holding discipline and regret, not alpha** |

**Also closed:** all six of the adversarial review's ranked next moves — **four
of them were already closed before this session began**, and I called one open
by reading the receipt the review quoted (`W9…run09`) instead of the newest of
31 (`run40`: n_trials **307**, DSR **0.1239**). The one genuinely open item, the
size-matched control, is now published: the arm beats a cap-decile-matched draw
at **t 2.496** and does **not** beat an equal-weighted market (**t 0.859**).

## CLAIMS FOR FABLE TO ATTACK

1. **The CUDA numbers were always real.** The whole proof is that a
   `site-packages` mtime cannot move without an add/remove. If it can on
   Windows, the downgrade hypothesis returns. `S0_cuda_drift_run01.json`
2. **`target_rev_1m__xs` beats a cap-matched draw (t 2.496) and not an EW
   market (t 0.859).** Attack the decile construction and the choice to hold
   the cost series fixed. `S2_size_matched_control_run01.json`
3. **More supply-chain tape made customer momentum weaker.** Attack the
   extraction: resolution is 31.1%, and the 68.9% residue is *assumed*
   non-US-listed. If the unresolved counterparties are systematically the large
   ones, this is a different graph, not the same graph on a different era.
   `W4b_companyworld_rerun_run01.json`, `S5_companyworld_integrity_check_run01.json`
4. **B10 not earned.** The pretrained ensemble is +8.14%/yr at t 2.243 and
   fails only the family correction. Say whether the family should be larger
   (verdict stronger) or per-arm (weaker). `W3b_neural_floored_run01.json`
5. **The mandated baseline is five months.** If true, every "beats/loses to
   lgbm" statement in this repo compares against a beta-timing artefact. Same
   receipt.
6. **The LLM cannot read 2016-19 filings into a beating rank.** Attack the
   POWER, not the rewriter — MDE is 8.75%/yr on 48 month blocks. (The
   rewriter-degradation confound I first published here was **retracted
   in-session**: it was a checker counting field names as data; true
   preservation is 97.9%/98.4%.) `L10_era_replay_v2_run01.json`
7. **Nine of the memory's twelve SUPPORTED cells are in a superseded family and
   all nine survive the row-level rule.** Is wholesale exclusion right, or
   should the corrected re-runs come back under a narrower rule?
   `B6_evidence_to_registry_run01.json`
8. **W4b's three receipts named the wrong edge file**, and only `source_rows`
   disagreed. Fixed and proved red. Does any *other* receipt stamp a module
   default in place of an argument? `S3_graph_receipt_provenance_run01.json`
9. **Four of the review's six moves were already closed.** If any of the four
   is in fact still open, my correction record is wrong the same way I was
   wrong about W9. `S1_panel_split_contamination_recheck_run01.json`
10. **The venue screener called seven names "the whole market."** The union
    gives 23, and **not one of the day's sixteen biggest movers was on the
    candidate list in either direction.**
    `B3_2_autopsy_and_opportunity_recall.json`

## LLM SPEND, TO THE CENT

DeepSeek **$3.98** (provider balance 13.36 → 9.38) + gpt-5-nano **$0.16**
(telemetry; no balance endpoint) = **$4.14 of $15.00**. By purpose:
`companyworld_edges` $2.12 of its $10 sub-cap, `era_replay_v2.decide` $0.48 of
its $5, everything else $0.00. 4,598 calls.

**The defect: our ledger prices only 56% of what the provider charged.**
Telemetry totals $2.22 against the provider's $3.98; the implied rate is
**1.81×** `LLM_PRICE_PER_MTOK["deepseek-v4-flash"]`. A **dollar** gate set at
$10 would in fact stop near $17.90 of real spend — the **call** ceiling is the
hard one, and it is what held. Not fixed from an inferred constant.
`S4_llm_spend_reconciliation_run01.json`

## WHAT I COULD NOT DO

- **The full 11,850-filing worklist was not extracted.** The pilot projected
  $11.71 against a $10.00 sub-cap, so the sample was cut rather than the cap
  exceeded; **2012-13 are absent** from the edge set.
- **B5 §3-§5 and B7 §3-§4 not started.** B5 §2 (permno↔CIK, 30,638 of 51,001
  filings, 60.1%) fell out as a by-product. One era ran in B7, so its three-era
  table is CANNOT DETERMINE by construction.
- **The Optimus MCP server was down for most of the session** — collateral
  damage from one agent's `taskkill /F /IM python.exe`, which also killed two
  other agents' jobs and cost ~1,676 already-billed extractions.
  `session_briefing()` and `aegis_verified_state()` ran at the start;
  `tools/refresh_aegis.py` ran at the end and succeeded (sha `da8875d`, 245
  session-memory pages, 470 doc pages).
- **Branch:** the tree was already on `lab/weekend-2026-09-06` when the session
  opened, so all 20 commits landed there. `main` is a strict ancestor; it is
  fast-forwarded at the end so both refs point at the same commit. Nothing
  rewritten, nothing pushed.

## THE ONE-LINE VERDICT

Three mechanisms bought, built and measured; none beat what we already had. The
most useful thing produced is not a candidate — it is that **the incumbent we
measure candidates against is itself five months of beta timing**, so the whole
comparison ladder has been resting on an artefact. That is the first thing the
next session should act on.
