# AI Panel — round 12 mid-round adjudication (2026-07-28)

**Inputs:** two external reviews delivered by Murat after the TRIAL-EVENT-8K-FILTER
result landed (module `b92a20f`, NEG_RESULTS §20; TEXT-LAZY pull in flight).
Review 1 = architectural ("stop testing anomalies, pivot to dynamic allocation").
Review 2 = technical (8-K successor control design, CIK-bridge audit, ceiling).
Adjudicated against the ledger. **Only the dispositions below are binding** —
the reviews themselves are hypotheses, not evidence (intake rules:
`EXTERNAL_RESEARCH_INTAKE.md`).

---

## 0. House assessment of the round-12 result (own view, stated before the reviews')

- **The 8-K disposition is correct and is the strongest methods receipt the
  program has produced.** An explore pass at t −7.06 was discarded because the
  pre-registered pseudo-event control returned t −11.33. Every frozen element
  earned its keep: the control arm (caught the selection), the attrition audit
  (named the mechanism — 32% vs 64% event survival, driven by ONE filter:
  eligibility-at-formation), the calendar-time cross-check (same verdict from
  the other side), and the refusal to open confirm on an uninterpretable
  explore. **This is a paper exhibit**: a two-arm design catching a "clean
  pass" that every single-arm study of 8-K drift would have published.
- **Not invoking the kill clause is also right.** The registered kill condition
  (non-negative cohort) did not occur; declaring family death anyway would be
  post-hoc. UNADJUDICATED is the honest state — successors need a valid control.
- The `master.idx` near-miss (loose glob matched the banned dateless quarterly
  aggregate; only SEC's 403 stopped silent ingestion into a PIT event set) is
  the house silent-fragility pattern in a new costume. The hardening (regex-pin
  to `master.YYYYMMDD.idx`, >2% day-failure raises) is the right shape: loud
  failure over quietly thin cohorts.

---

## 1. Review 2 (technical) — dispositions

| # | Claim | Disposition |
|---|---|---|
| 2.1 | 8-K successor needs a **matched-sample control** (nearest-neighbor non-filer matched on sector, size, prior-12m return), not just pre-event eligibility freezing | **ADOPTED as successor design REQUIREMENT** (post-freeze, NEW registration only). Combined with the run session's own fix (eligibility frozen at a pre-event date): both go in. The filer-minus-matched-non-filer spread is the only estimand that isolates filing-date information from distress persistence — exactly the confound §20 measured. |
| 2.2 | **CIK-bridge sanity gate** before TEXT-LAZY: check bridged firms' cap-decile/delist profile vs CRSP natively | **ADOPTED as a data-quality audit — REPORTED-never-deciding, no spec change.** The bridge covers 88.5% of universe permnos; the audit asks whether the missing 11.5% (plus dropped-ambiguous) are random in size-decile and delisting status or concentrated in dead/micro names. Biased coverage → interpretability caveat on the run (the 8-K attrition-audit pattern), it does NOT move any bar. **Must run BEFORE the explore result is interpreted.** One correction to the review: the bridge was already built survivorship-neutral (two historical registries, `company_tickers.json` explicitly rejected) — the audit VERIFIES that property, it doesn't add it. |
| 2.3 | Don't burn a ceiling re-registration at 158; freeze on the existing measurement | **ADOPTED** (converges with the run session's own flag). The freeze note must state: search converged at 158 (+ whatever TEXT-LAZY's bookkeeping adds), below the ~196 threshold round 5 set as the next legitimate re-check; the existing INSTR-OVERFIT-CEILING zero-skill envelope (E[max t] ≈ 3.6–4.0) remains the operative alarm. Stopping BEFORE the alarm threshold needs no new registration — and inventing filler hypotheses to reach 196 would be the exact self-deception the ceiling exists to measure. |
| 2.4 | Is TEXT-LAZY two-sided (filing divergence as obfuscation/short signal)? | **Answered by the frozen spec — one direction, no re-spec.** Registered: `text_cos`(+)/`text_jac`(+), long non-changers. The divergence-as-distress story is NOT an arm of this trial; it exists only as the pre-declared, bounded FILTER fallback (changer cohort t ≤ −2.0 in the same explore run → permits a NEW successor registration). Factory scans report both tails mechanically, but nothing outside the frozen rule can graduate or kill. |

## 2. Review 1 (architectural) — dispositions

**Header finding:** the review's direction — allocation-level research over more
anomaly tests — **converges with decisions this program already adopted in
rounds 8–10** (REGIME-ANALOG adoption, belief-engine spec, freeze plan). That
convergence from an independent reviewer is worth banking. But its scorecard is
factually wrong about this ledger in two places, and the correction matters:
**"untested" (2/10) and "tested-and-rejected" are different epistemic states —
the second is an asset.**

| # | Claim | Disposition |
|---|---|---|
| 1.1 | "Dynamic allocation / decision engine: 2/10 — missing field" | **REJECTED as a reading of the ledger.** The allocation program is round 11's headline: JM1 + JM2 regime rotation tested and REJECTED at held-out confirm (NEG_RESULTS §15/§18 — two receipts that single-trigger regime gates fail out-of-sample); TSMOM-XA tested, CONFIRMED, and live as a paper lane pair since 07-27; REGIME-ANALOG belief engine LIVE (283 BeliefStates, descriptive). The direction is adopted; the score is wrong. |
| 1.2 | Next mission = "when should each anomaly receive capital?" | **DECLINED as a mission rewrite.** Anomaly-timing is conditional-anomaly space: candidates × regimes is a combinatorial deflation bomb, and the factor-timing literature is itself weak. Our own receipts (JM1/JM2) point the same way. Specific timed-allocation hypotheses are admissible POST-FREEZE as NEW deflated registrations with post-hoc-repair provenance declared — same bar as everything else, no blanket pivot. The review's supporting citation (SSRN 6962678) already failed verification in round 11 and stays unverifiable. |
| 1.3 | "Exit research is missing / almost none on SELL" | **REJECTED on receipts:** INSTR-HOLD-HORIZON answered the exit question for the factory class (signal-band exits make long holds free; 14–17 bps/mo flat across 1–24mo bands); TRIAL-EXIT conservative-ATR lane live since 2026-06-17; TSMOM-XA is exposure-management machinery. What IS true: no cross-sectional SELL-signal family (revisions deterioration, insider-sale clusters, breadth) has been registered — admissible post-freeze; note the 10b5-1 2023 break makes insider-SALES designs pre/post-2023 unsafe from the start. "Sell signals are often easier than buy signals" is an assertion without a receipt; it earns a prior of exactly zero. |
| 1.4 | Belief updating with confidence + **explicit evidence-weight hierarchy** (forward 10 / confirm 8 / explore 3 / literature 2 / LLM 0.5) | **ADOPTED (brain layer, descriptive).** The BeliefState ledger already carries confidence trajectories + evidence lists; the weight schema is a clean codification and lands as a REGIME-ANALOG **phase-2 spec item** alongside the already-queued Bayesian updating. Weights are a reporting convention — nothing allocates off them. |
| 1.5 | Causal graph brain (reason over relationships, not documents) | **REFUSED at current corpus size** — same ruling as vector-DB/Neo4j in round 6 (~50-doc corpus). Deeper objection: causal graphs encode narratives, and JM2's lesson (§18) is that encoded stories about events fail out-of-sample. Revisit only with a concrete decision a graph would change that retrieval doesn't. |
| 1.6 | Corporate event graph → research event **sequences** ("CEO leaves → guidance cut → …") | **DESIGN-NOTE ONLY.** Sequence mining is uncontrolled multiplicity (every path through the graph is a candidate), and round 12 just demonstrated that even SINGLE-event cohorts are selection-contaminated without matched controls. Admissible post-freeze ONLY as: one pre-registered sequence hypothesis + the 2.1 matched-control design + deflation. |
| 1.7 | No neural nets now; trees/Bayesian/state-space instead | **CONVERGENT** — INSTR-VOC receipt + round-8 unanimous refusal. No action. |
| 1.8 | LLM for perception (filings, events, timelines), never prediction | **ALREADY HOUSE LAW** (LLM narrates, never allocates; NEG_RESULTS §19 external receipts). No action. |
| 1.9 | Hierarchical allocation stack (macro→sector→factor→stock→sizing→exit) | **PARTIALLY CONVERGENT.** As research: this IS the post-freeze allocation program, one registrable instrument per layer, sequenced below. As an architecture diagram: not actionable by itself. The product (Goal B) already serves most layers descriptively. |
| 1.10 | "Freeze anomaly discovery unless a genuinely new information source is introduced" | **CONVERGENT — this is the freeze**, already scheduled this round. The "new information source" exemption matches the house rule that a closed family reopens only against its specific receipt. |

## 3. Bindings on the run session (relay to the Opus session before it interprets TEXT-LAZY)

1. **2.2 first:** run the CIK-bridge coverage audit (size-decile + delist-status
   profile of bridged vs unbridged universe permnos; ambiguous-drop count)
   BEFORE reading the explore result. Banked next to the result, never deciding.
2. **2.3 at the freeze:** freeze at the converged count on the existing ceiling
   measurement; no re-registration below ~196; state it explicitly in the
   freeze note.
3. **2.1 + 1.6** bind any future 8-K/event successor registration (matched
   non-filer control + pre-event eligibility, one pre-registered hypothesis).
4. **1.4** lands in the REGIME-ANALOG phase-2 spec, not as a new trial.

## 4. Paper impact

The 8-K control-arm catch joins the exhibit list: method lead (the wall + the
two-arm design), empty cost-killed cohort, −0.544 as supporting replication of
McLean-Pontiff Fig 1.B, KO cost model, **and now "the control that beat the
treatment"** — the cleanest illustration in the whole ledger of why single-arm
event studies over-report.
