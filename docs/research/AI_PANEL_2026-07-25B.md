# AI panel — round 5 adjudication (2026-07-25 PM)

Two reviews pasted by Murat (R1 = "instructions for the three builds";
R2 = "next steps for the investor brain" prioritized plan). Adjudicated
against the ledger. The three-build sequence (daily macro harness → FDA
daily-CAR → tgt rebuild) matches the standing queue and is ADOPTED as the
execution order; build 1 executed this session (INSTR-MACRO-BATCH4).

## Adopted

- **Sequential three-build order** (R1): daily macro harness first, FDA
  daily-CAR second, tgt-price rebuild third — each its own freeze commit.
  Note: paths corrected to house layout (`aegis_brain/macro/...` in the
  research module; the reviews' `src/brain/factory/` does not exist).
- **Jump model with explicit λ penalty** (R1#1): already the round-4
  amendment; implemented today (`aegis_brain/macro/jump_model.py`), λ=50
  primary frozen with {10,100} sensitivity, causal forward filter. VIX as a
  feature was NOT adopted this round — no daily VIX on disk; realized-vol +
  downside-deviation features (closer to Shu-Yu-Mulvey anyway) instead.
  A VIX feature variant = future registration if the primary shows promise.
- **FDA daily-CAR design inputs** (R1#2, R2#1): T+1..T+20 drift as the
  primary tradeable claim; day-0/[-1,+2] reaction reported as descriptive
  (R2's short window answers "is there a reaction", not "is there a
  tradeable drift"); beta from a [-120,-30] pre-event window; volume-based
  attention gate as the exploratory secondary arm (round-4 framing stands).
- **Dual-gated target-price signal** (R1#3): long high-implied-return AND
  low-dispersion only — matches the round-4 PSZ adoption verbatim.
- **Rate-cut vulnerability screen** (R1 novel #1) — NEW batch-9 stub:
  short-term/floating debt exposure (dlc/at from funda) as a CONDITIONAL
  quality screen. Mechanism plausible (refinancing fragility), low
  turnover. Needs a rate-cycle conditioning variable (FFR from FRED) —
  registrable from on-disk + free data.
- **Index-inclusion front-running (R1 novel #2) — stub, DATA-BLOCKED:**
  needs PIT S&P membership/announcement dates; not on disk, historical
  announcement timestamps not freely available survivorship-clean. Goes to
  the WRDS want-list (Compustat idx constituents) before any registration.
  Also flagged: the classic inclusion pop has DECAYED sharply post-2010
  (index-effect literature) — prior MEDIUM-WEAK.
- **SBCORR gate** (R1 novel #3): already registered round 4; the
  dip-buying-conditional read is in today's descriptive run.

## Refused (receipts)

- **hmmlearn as the regime engine** (R2#2): round-4 amendment stands —
  jump model primary. Re-proposal one day after adjudication.
- **Re-run INSTR-OVERFIT-CEILING "now that new signals are added"** (R2#5):
  reruns are forbidden (one-shot law). A SECOND ceiling registration
  becomes legitimate when the library grows materially (declared: +50
  candidates, i.e. at ~196 cumulative). The 53-signal receipt stands.
- **Target-price dispersion sign per "Li et al. 2021: high dispersion →
  HIGHER returns"** (R2#3): the round-4 verification found the two-sided
  literature (PSZ 2025 conditional-negative vs 2021 sort-positive). The
  registered design is the PSZ CONDITIONAL long-only leg; an unconditional
  dispersion sort is NOT adopted — the sign dispute is exactly why the
  conditional design was frozen. No change.
- **Opportunistic-insider implementation** (R2#4): already BRAIN-003 —
  promoted, live, forward clock to 2027-07. The review re-describes our
  existing trial as future work. Role-weights stub stands from round 4.
- **USAspending for DoD events** (R2#6): rejected round 2 — ~90-day OPSEC
  delay, no announcement timestamps. INSTR-DOD-FWD stays forward-only.
- **News-sentiment / Google-Trends backtests** (R2#7): barred by PIT
  policy (no historical point-in-time news corpus) — forward-only, as
  standing.
- **vectorbt / external backtest engines** (R2 tooling): keep the house
  harness (adjudicated at V3 research: our cost/turnover/PIT mechanics ARE
  the product; a generic engine hides them).
- **"Buy 20 days prior to official rebalancing dates"** without PIT
  announcement data is exactly the look-ahead class the factory exists to
  kill — folded into the data-blocked stub above, not runnable as stated.

## Noted for later (no action now)

- OptionMetrics IV signals (entitlement catalog exists, 578 tables) — WRDS
  want-list, attended session.
- Buyback/spinoff event studies — already batch-3 leftovers, queued.
- Risk-parity overlays + investor-UI features — product side (chunks 5/7),
  parallel track, not research trials.

Cumulative candidate count unchanged by this round (macro instruments are
instruments, not cross-sectional candidates): **146**.
