# AI Panel Round 3 — 2026-07-24 EOD (GPT / Gemini / Copilot / DeepSeek)

Responses to the Batch-5 / hold-horizon / FDA milestone. Compact adjudication;
same rules as rounds 1-2. Full receipts in `Aegis module/docs/STRATEGY_FACTORY.md`.

## Adopted
- **GPT: horizon-first registration rule** — every new hypothesis must declare
  its expected holding horizon AND why the edge decays on that horizon before
  it enters the factory. Adopted as a standing registration field from Batch 6.
- **GPT: GPR as regime filter, never stock picker** — already the Batch-4
  design; reaffirmed.
- **GPT/Gemini: disposition-aware selling** ("sell on signal decay, never on
  P&L status") — already receipted by INSTR-HOLD-HORIZON; promoted to a
  chunk-7 product principle.
- **Gemini: DTC-squeeze × quality interaction** — registered in Batch 6.
- **Gemini: capital-gains overhang (Grinblatt-Han 2005)** — published, never
  tested here, computable from price/volume; registered in Batch 6.
- **Gemini: customer-concentration (Patatoukas 2012)** — computable from
  seg_customer salecs + funda sale; registered in Batch 6.
- **Consensus: insider cluster arms under the FLAG harness** — registered as
  TRIAL-BRAIN-009 (the batch-5 decile scans were VOID-DESIGN, this is the
  correct instrument).

## Rejected / already adjudicated (not reopened)
- DeepSeek #6 accruals ("still alive post-2010") — batch 2 measured it
  INVERTED (t ≈ −2.9/−3.1). Second panel to re-propose it; the receipt stands.
- DeepSeek #8 monthly supply-chain momentum — adjudicated DEAD both arms
  (batch 3b + TRIAL-THEME-SUPPLY §12) the same day this advice arrived.
- DeepSeek #9 news-contrarian — PIT news policy bar (forward Brier only).
- DeepSeek #12 sector-neutral momentum — momentum-family closed for window.
- DeepSeek #10 / GPT #4 target-price rules — the entire target-price family
  is VOID pending `ibes.adj` (both directions retracted); nothing registrable.
- Copilot #4-6 (DDQN/RL, genetic programming, hybrid-ML allocators) — canon:
  no RL/NN allocators on ~276 monthly obs; INSTR-RL-ALLOC stays gated.
- Gemini #5 asset-growth × ROE — both components tested batch 2 (asset growth
  inverted); interaction carries a strongly negative prior, not queued.
- Gemini #9 low-IVOL screen — defensive composite already adjudicated
  screen-class; it is a lane-risk input, not a new candidate.

## Blocked on data (queued, not registered)
- Away-from-consensus rec changes (needs `ibes.recddet` analyst-level detail
  — added to WRDS list), R&D capitalization (`xrd` in batch-4 fetch), BoardEx
  centrality (batch-4 fetch), bias-corrected targets (`ibes.adj`).

## Corrections to the panel
- DeepSeek: INSTR-OVERFIT-CEILING is a monthly full-sample in-sample fit
  (fragility measurement) — it needs no high-frequency data and no XGBoost
  meta-learner; and the FDA reject t was −0.89 (net), −0.68 was gross.
- Gemini's Q ("daily harness vs flag harness first?"): the flag harness
  already exists (`harness/flag_portfolio.py`) — insider arms run today;
  the daily harness is the only genuinely new build (Batch-4 macro).
