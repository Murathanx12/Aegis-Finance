# AI Panel — round 11 (2026-07-26): the allocation program executed (JM2 REJECT + belief engine LIVE)

Three reviews adjudicated (DeepSeek, GPT, Gemini), then the round's build
ran: INSTR-REGIME-JM2 (one execution, both windows) and INSTR-REGIME-ANALOG
phase 1. Module commits: freeze `470ed0f` → JM2 results `3c254ec` → engine
`d73a502`.

## Citation verification (one agent + house rules)

| Claim | Verdict |
|---|---|
| GPT: Chen & Welch, "What Useful Alphas?" (SSRN 7071598) | **REAL** — arXiv 2607.06502, drafted 2026-07-08. ~200 published anomalies; median post-2005 non-microcap return **7 bps/mo**, killed by modest costs/luck. **ADOPTED as a paper reference**: independent, contemporaneous corroboration of the factory's converged finding and the CZ-CALIB fame-decay exhibit (−0.544). |
| GPT: "When Do Anomalies Deserve Capital? Macro-Regime Memory…" by Taheri Hosseinkhani (SSRN 6962678) | **UNVERIFIED — likely hallucinated.** The author exists on SSRN (AI-survey papers); this title is unfindable anywhere. The idea (route capital across anomaly families by regime) is fine and already the house direction — but it carries **zero literature weight** and must not be cited. |
| GPT: AQR "A Century of Evidence on Trend-Following" | Real (Hurst-Ooi-Pedersen), already in the corpus since the TSMOM-XA registration. |

## DeepSeek — hallucinated receipts corrected (2 items)

1. **"Belief engine (descriptive) built: `macro_analog.py` … `BeliefState`
   artifact class added"** — claimed as ROUND-10 output. FALSE at the time
   of the review: neither existed until this session (receipt: the
   `aegis_brain/macro/` listing at session start contained only
   `jump_model.py` + `daily_harness.py`). DeepSeek narrated the next
   session's *plan* as already done — the same "future-as-receipt" failure
   mode round 8 caught (INSTR-REVERSAL). The files now exist because we
   built them today; that does not retro-validate the claim.
2. **"155 (was 153; +2 from Build 3 upgrades and two miscounts)"** — wrong
   provenance. +2 = TRIAL-TGT-REBUILD's two pre-registered REJECT arms
   (#154 tgt_upside, #155 tgt_ld). No miscounts, no upgrades.

Everything else in the DeepSeek summary matches the ledger.

## Gemini — clean (zero hallucinations), one nuance

"The blunt 25 bps flat cost was **completely innocent**" — half right.
The empty cohort proves no signal died *solely* from the flat wall; but
INSTR-COST-MODEL showed the flat 25 was over-penalizing largemid (~3-4 bps
realistic), which is exactly why the remeasure was admissible at all.
Innocent of murder, guilty of over-charging; both documented.

## GPT design proposals — adjudicated one by one

| Proposal | Verdict |
|---|---|
| Retrieve → **cluster** → explain (episodes, not days) | **ADOPTED + BUILT** — episode clustering (126td transitive merge) is in phase 1. |
| Uncertainty / analog-agreement → confidence | **ADOPTED + BUILT** — confidence = fwd-6m sign-agreement × (1 − mean distance percentile); it demonstrably sags in stress (0.43-0.60) vs calm (0.76-0.81). |
| Bayesian prior→evidence→posterior layer | **DEFERRED to phase 2** (registered as such). A daily posterior is the trajectory record; the update rule is a design choice that must be registered before it ever feeds anything. |
| "Failed thesis" mechanism (beliefs auto-decay when implications fail) | **DEFERRED to phase 2** — this is the PDUFA-ledger pattern applied to beliefs; phase 1 stores timestamped beliefs precisely so forward scoring becomes possible. |
| Allocation Factory (~50 policies: cash, sector rotation, vol targeting, DD response…) | **ADOPTED as the post-freeze Phase II framing.** Matches the house queue; each policy = a pre-registered walled trial. JM2 (run today) was the first — and it died. |
| "Officially close Phase I now, write the paper" | **MODIFIED-REFUSED.** The freeze comes after batch 10 (8-K event-flow — PIT-safe news, already prior-checked clean) and the ceiling re-registration ~196. Skipping the ceiling re-check would leave the paper's central honesty exhibit stale. |

## Build 1 executed — INSTR-REGIME-JM2: **REJECT** (module `TRIALS/INSTR-REGIME-JM2.md`, NEG_RESULTS §18)

The round's headline. Registration declared post-hoc-repair provenance
(explore ≈ zero weight; confirm weakened; forward only clean test) — and
the declaration was EARNED in both directions:

- **Explore 2004-2018 flattered the repair**: CAGR 12.2% vs SPY 7.7%,
  maxDD −20.4%, calendar-2008 +32.4%, better than JM1 on every line. Had
  explore carried weight, JM2 graduates.
- **Confirm 2019-2024: REJECT — the gate made 2022 WORSE than the design
  it repaired** (−23.9% vs JM1's −21.6%; t JM2-vs-JM1 −1.18; both frozen
  bars missed). Mechanism: 2022's bond crash was real-rate-driven —
  T10YIE peaked in April 2022 and FELL through the worst of the TLT
  collapse, so the inflation-momentum gate routed risk-off capital back
  INTO TLT. Only 9.5% of confirm risk-off days were gated away. No
  lookback (63/126/252) rescues it.

**Lesson with teeth: a post-hoc repair does not even guarantee fixing the
crash it was built for — the repair encodes your story about the event,
not the event.** Single-trigger regime rotation (state machine + one macro
gate) is CLOSED with two receipts. Successors need a different information
class (the analog engine's multi-feature state estimate is the obvious
candidate) and start as new walled registrations inheriting both receipts.

## Build 2 executed — INSTR-REGIME-ANALOG phase 1: **LIVE** (descriptive, never arms)

`aegis_brain/macro/macro_analog.py` + `scripts/run_regime_analog.py`,
7 unit tests green. 6,053 complete descriptor vectors (2002-07 → 2026-07;
disk-SPY constraint binds, disclosed). **283 BeliefStates** in
`ledger/belief_states.jsonl` (282 month-end backfill + current).

Data departures (disclosed pre-results): FRED truncated HY-OAS to a
rolling ~3y window (ICE licensing) → credit features use DBAA−DGS10;
LBMA gold fix removed from FRED → GC=F futures.

Face validity: 2020-03-31 retrieved the GFC + 2002 bear as top episodes;
**2021-12-31's top analogs were the 2017→2020-01 melt-up and Oct-2007 —
the prior cycle top, one month before this one**; confidence sags in
stress. Current state (2026-07-24, confidence 0.81): nearest episodes
Oct-2018 / late-2021 / 2016-18 grind; analog frequencies fwd-6m positive
84%, fwd-12m maxDD ≤−15% in 35% of analogs; sector winners XLE/XLK.
Estimates-not-forecasts, labeled as such in the artifact.

## Batch-10 prep (prior-checks only, per queue)

- 8-K event-flow family: prior-check CLEAN (hits = its own queue entries).
- Zombie/rate-cut exclusion screen: **DATA-BLOCKED — `xint` is not in any
  on-disk Compustat pull** (comp_funda has ib/dp/dlc/dltt, no interest
  expense). Registration defers to the next attended WRDS tap; xint on
  the want-list. Ceiling re-check NOT touched (155 < ~196).

## Queue (post round 11)

1. **Batch 10:** 8-K event-flow registrations (the PIT-safe news family).
2. Overfit-ceiling re-registration at ~196 → **FREEZE** → paper (new
   reference banked: Chen-Welch 2026).
3. Phase II (post-freeze): allocation policies as walled trials; ANALOG
   phase 2 (Bayesian updating, failed-thesis scoring, evidence graphs,
   insider feature).
4. Attended (Murat): **TSMOM-XA lane seed** (seed-a-lane, env flag,
   lane-integrity both sides — do it together at the keyboard); PDUFA
   SCPH scoring ~late Aug (`scripts.ledger_score`); conviction-lane
   decisions; quarterly CMP/SMQ refresh ~Oct; Monday GPR snapshot;
   next WRDS tap wants `xint`.
