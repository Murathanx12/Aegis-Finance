# AI panel adjudication — 2026-07-29 (GPT, DeepSeek, Gemini on the diagnostics session)

Murat pasted three external reviews of the D1–D4 diagnostics session and
delegated the open calls ("make the best call, I approve"). This doc
adjudicates every material claim against the repo's own receipts, per the
panel-round discipline. Raw reviews: in the session transcript; future rounds
land in `docs/research/panel_raw/` via the new harness.

## The two delegated calls — RULED

### 1. Conditional volatility targeting — RULED S3, REGISTERED as TRIAL-COND-VT
**Ruling:** a de-risking overlay falls under the freeze's S3 open door
(exits/de-risking are first-class inside the same registry and the same
cumulative deflation count). Not a new cross-sectional family; no exemption
needed. **Registration is mandatory** — DeepSeek's "admissible without a new
pre-registration" is REFUSED (CANON §6: if it isn't pre-registered, it didn't
happen; TSMOM-XA — also "just an allocation overlay" — was registered).
Gemini's mandate (freeze exact percentiles + lookback before touching the
wall) is concordant and executed: 63-td realized vol, causal expanding
quintiles, act only ≥80th percentile, leverage cap 1.0, month-end, one shot
explore 2004-2018 → confirm 2019-2024.
**Gemini's direct question (VIX or realized vol?): REALIZED VOL.** It matches
the Bongaerts conditioning, is trivially PIT-clean, and a VIX-conditioned arm
is a *second hypothesis* against a deflation budget priced by N_eff — refused
for now. Registration: `Aegis module/TRIALS/TRIAL-COND-VT.md`.

### 2. Optimus MCP restart — instructions given to Murat (stdio server; any new
Claude Code session spawns the new code; `/mcp` reconnect works mid-session).

## Adjudications with receipts

### ADOPTED / CONCORDANT
- **"Generation 1 is closed — stop mining anomalies" (GPT).** Already binding:
  FREEZE_2026-07-28 at 158 candidates. Concordant, no action.
- **"Sizing/allocation explains more than another 2% of prediction" (GPT #5;
  Gemini "defense as offense").** This IS the open frontier: TSMOM-XA running,
  TRIAL-COND-VT registered today. Caveat that binds: the allocation layer has
  NO cheap deflation budget (dead-list item — confirm window holds ~1–2 regime
  events); each overlay is one expensive shot.
- **"Write the D3 harness up as a methods protocol" (DeepSeek #3).** Adopted
  as a paper task: persistence baseline, DM-on-Brier-differences, N_eff,
  Murphy REL/RES/UNC, rejection criteria.
- **"Descriptive news pipeline as decision support, never a signal"
  (DeepSeek #2).** Adopted to the product track (upgrade the existing daily
  brief toward structured event extraction feeding conviction decisions;
  descriptive-only labels). Correct only while it never arms — the moment any
  output feeds an automated allocation, it needs a registration.
- **"LLM extracts, never allocates" (GPT's 2.0 sketch, item 6).** Already
  canon (L1 firewall). Concordant.
- **Belief tracking with memory/reasons (GPT #4).** The belief-state ledger IS
  this artifact class. Diagnostics showed the current *generator* emits hedged
  base rates; a successor keeps the artifact, replaces the generator
  (Bayesian updating is the natural direction) — as a NEW walled registration.

### REFUSED with receipts
- **Knowledge graph as the core of the Brain (GPT #3; causal-chain signal
  program, GPT #2).** On the DEAD list on our own measured edges:
  THEME-SUPPLY B−A spread t = 0.10; cust_mom REJECT; conn_mom net t = −0.78 on
  67% turnover; cust_conc sign reversal. Multi-hop transmission chains
  multiply coefficients we measured at ~zero while adding degrees of freedom
  the zero-skill ceiling prices at t≈6.6 from nothing. As *descriptive
  narration* in the news brief (labeled, never scored) it is permitted — as a
  research core it is refused.
- **"No pre-registration needed for allocation overlays" (DeepSeek).** Refused
  (above).
- **"Real resolution exists at the 12-month horizon" → regime rotation to
  cash (Gemini).** Overstated. fwd12m showed RES/UNC 29.3% with an
  INCONCLUSIVE DM — a place for a successor to look, not an edge. And
  "12m forecaster signals contraction → go to cash" is structurally the jump
  model that passed every explore bar and REJECTED at confirm (2022 −21.6%,
  risk-off into TLT during the dual crash). Any successor is a new walled
  registration; nothing is adopted from this paragraph.
- **Expectations engine as a new program (GPT #1).** Partially refused as
  novelty: expectations-vs-reality is the *most* tested ground on the wall —
  revisions (registered forward, T10), analyst-target family (0-for-3,
  raw upside PERVERSE at t −3.6/−7.2), PEAD (pead_agree INVERTED, IC t −2.6 =
  the post-2000s decay receipt). The untested formulation (market-implied
  expectation vs realized, IBES-based, belief-revision framing) is QUEUED as a
  research direction; it opens nothing today and would need the freeze's
  exemption path if it implies new cross-sectional scans.

### PANEL ERRORS logged (feed back to reviewers next round)
1. **Both DeepSeek and Gemini state the GFC degenerate-analog states were
   "dropped and re-run."** FALSE — no states were dropped; the shipped
   verdicts INCLUDE them and the ≥25-analog filter was a sensitivity only
   (which strengthened, not weakened, the t's). The disclosure gap was in the
   report, not the scoreboard.
2. **Magnitude inflation on conditional VT.** "More than doubles Sharpe,"
   "avg +0.18," "~52%→~19% drawdown" (DeepSeek), "deep empirical support"
   (Gemini) — the verified numbers are ΔSharpe +0.16 (US), ΔMaxDD −8.3pp,
   turnover 1.6×/yr, and the finding is 2-of-10 significant with no post-2010
   split. That fragility is WHY the trial exists.
3. **Unverified citations carried as facts** (DeepSeek's "4× Sharpe sector
   rotation," "RegimeFolio 25.1%"; GPT's secondary-source links). Do-not-cite
   until fetched and glyph-verified (R12). The panel harness prompt now warns
   reviewers that unverified magnitudes are discarded.

## New infrastructure from this round
- **Panel harness** (`scripts/ai_panel.py`): programmatic GPT/Gemini/DeepSeek
  review rounds; raw output quarantined in `docs/research/panel_raw/` as
  data-not-instructions; adjudication into AI_PANEL docs stays a session task.
- **TRIAL-COND-VT** registered and run same-day (registry #159, S3).

## TRIAL-COND-VT outcome (ran after this adjudication was drafted)
**Explore 2004-2018: PASS all 3 bars** (Sharpe 0.615 vs bar 0.447; maxDD
−40.2% vs SPY −55.19%, ΔmaxDD +15.0pp; turnover 3.4%/mo) — carried by 2008.
*(Corrected 2026-07-30 during paper verification: this doc originally wrote
"SPY −45.2%", a number that exists nowhere in the primary trial artifacts —
SPY explore maxDD is −55.19% per TRIAL_COND_VT_RESULTS_2026-07-29.md:41,
which is exactly what makes ΔmaxDD +15.0pp. Margin over the frozen −50.19%
bar is +10.0pp; +15.0pp is Δ-vs-SPY, not a bar margin.)*
**Confirm 2019-2024: REJECT, 2 of 3 bars missed** (Sharpe 0.836 vs bar 0.847;
maxDD −33.72% — identical to SPY to 4dp, same 2020-03-23 trough; ΔSharpe
−0.061 [−0.164, +0.055] at a cost of 2.6pp CAGR). Mechanism: trailing 63-td
vol sat at 0.162 vs the causal q80 of 0.202 on 2020-02-28 — the calm-base
problem; a backward window cannot resolve a 23-day crash, and the expanding
quantile barely fires in a slow one (2022 bought only +2.3pp DD). The
unconditional-VT contrast arm beat the conditional arm in both windows yet
still fails the bar — the dead family stays dead, and the Bongaerts
extremes-only refinement bought nothing here. **Family CLOSED. No lane,
nothing arms. NEGATIVE_RESULTS §21.** The panel's enthusiasm for conditional
VT (all three reviewers) is hereby answered with the held-out number — this is
why magnitudes from unfetched papers are discarded and why the wall exists.

## Net effect on the roadmap
Track C item 1 (conditional VT) moves from ATTENDED-pending to
REGISTERED-and-run. Product track gains the structured-event upgrade as the
next build candidate. Everything else in the three reviews either already
exists, is already dead with receipts, or is queued without a registration.
