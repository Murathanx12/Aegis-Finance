# AI panel — round 6 adjudication (2026-07-25 EOD)

Two reviews (R1 = "epistemic memory architecture" + JM successor; R2 =
"project progress and findings" + brain enhancements), including the
requested review of the brain/Optimus module. Adjudicated same day as
Build 2 executed.

## The brain review — reality check first

Both reviews describe the brain as "an amnesiac scientist" with "static
Markdown ledgers" and no memory. **That premise is substantially wrong:**
the Optimus MCP already serves `aegis_registry` (every trial row),
`aegis_canon` (guardrails), `aegis_postmortems`, and `brain_query` (corpus
retrieval over the ingested repo), and the adjudication loop has refused
10+ re-litigations across six panel rounds using exactly those ledgers.
The house does have epistemic memory; what it lacked was a *mechanical*
retrieval step at registration time. Verdicts on the proposals:

- **Prior-check gate: ADOPTED, lightweight** — `scripts/prior_check.py`
  (module): keyword retrieval across registry + all trial docs + factory
  protocol + taxonomy + NEGATIVE_RESULTS + panel docs, run BEFORE any new
  registration; procedural rule added (no registry row without a
  prior-check pass). **ChromaDB/FAISS/Pinecone REFUSED at this corpus
  size** (~50 documents; keyword search is exhaustive and auditable).
  Revisit threshold declared: ~500 documents.
- **Dynamic constraint meta-learning: ALREADY EXISTS in manual form** —
  `SIGNAL_TAXONOMY.md` IS the clustered graveyard (turnover-kill class,
  book-without-rank class, IC-real/net-dead shelf), and its lessons are
  already constraints (horizon-first + 4-tag registration rules).
  Automating the clustering adds nothing at 146 candidates. No-op.
- **Knowledge graph (Neo4j), specialist agents, FinMem/FinAgent layering:
  DEFERRED to chunk 6 (LLM brain v2)** as design inputs — correct
  destination, wrong week. Neo4j/Snowflake/Postgres infra REFUSED (house
  rule: no database; the research module is files, the prod API is
  stateless + cache).
- **"Register API keys so agents can query on demand": no-op** — keys
  exist in prod (Railway env), and key handling stays out of chat by law.

## Executed this round (Build 2)

**TRIAL-BRAIN-011 FDA daily-CAR: REJECT** (`f6d1648` freeze → one shot).
CAR(+1,+20) +2.1% but t 1.45 vs frozen bar 2.0; drift concentrated in days
1-5, small-half, and HIGH-attention events (+3.6% vs +0.6%) — **the
low-attention gate both panels proposed is refuted in its own arm.**
Confirm gate never opened. FDA approval drift closed at both resolutions
(NEGATIVE_RESULTS §16). Crosswalk v2 formally signed off (Murat spot-check
+ programmatic validation: 134 rows, 0 date/overlap/ticker defects).

## Adopted → queued with registration requirements

- **INSTR-REGIME-JM2 (inflation-gated rotation)** — the JM successor both
  reviews converged on (T10YIE or CPI momentum routes risk-off capital to
  TLT vs GLD/cash). Registrable next session; the registration MUST
  declare its provenance honestly: it is a POST-HOC repair designed after
  seeing 2022 kill JM1, so its explore result carries near-zero evidential
  weight — confirm + forward clocks are the only meaningful tests. FRED
  T10YIE starts 2003 (fits the window); Ilmanen 2003 prior noted.
- **Zombie-firm / rate-cut vulnerability screen (merged stub)** — same
  family from rounds 5+6: interest coverage (EBIT/XINT) × short-term debt
  (dlc/at) as a LONG-SIDE EXCLUSION screen conditioned on rate trend.
  House does not short — the "short overlay on zombies" leg is refused as
  out of mandate; the exclusion version is batch-9 registrable (xint on
  the funda want-list check).
- **Analyst capitulation curve** — the long leg only (post-capitulation
  reversal after the largest-magnitude downgrade); shorting leg refused
  (mandate). Requires the detail-file revision sequences → folded into
  tgt-rebuild PHASE 2, after Build 3's base rebuild.

## Refused (receipts)

- **Option-skew signal** — third proposal; still OptionMetrics-blocked
  (want-list, attended session). Unchanged.
- **"Li et al. 2021: high TP dispersion → higher returns"** — third
  re-raise; the PSZ-conditional design is frozen (rounds 4-5). R2 also
  mislabels Diether-Malloy-Scherbina as target-price dispersion (it is
  EPS-forecast dispersion). No change.
- **vectorbt / FinRL / external engines** — second re-raise; house harness
  stands (cost/turnover/PIT mechanics are the product).
- **USAspending in the data stack** — third re-raise; OPSEC-delay receipt
  stands; DoD stays forward-only.
- **"Set the seed flag for the TSMOM lane" as an engine action** — seeding
  is ATTENDED by canon (env-gated, human flips the flag, lane-integrity
  checks both sides). It is Murat's action in a dedicated session, with
  the lane YAML prepared under the seed-a-lane skill — not a background
  step appended to a research day.

## Standing corrections to R2's summary of our results

- BRAIN-007 is insider × gp fusion (not "insider with quality"); BRAIN-009
  CLOSED clusters (they add nothing over single opportunistic buys) — R2
  cites the cluster design as "should be a priority" — already adjudicated.
- The overfit ceiling's honest-direction t≈3.3 was WITH costs (25 bps),
  not "without costs."

Scoreboard: cumulative explore candidates **146** + 1 event trial
(BRAIN-011) adjudicated; macro instruments: 1 confirmed survivor (TSMOM-XA,
defensive), 1 rejected (JM), 2 descriptive receipts. Next: **Build 3 —
target-price rebuild** (dispersion-conditioned, PSZ design), then JM2 +
batch-9 registrations, TSMOM lane seed (attended, Murat).
