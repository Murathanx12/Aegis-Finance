# AI Panel — round 12 second adjudication (2026-07-28, evening)

**Inputs:** two further external reviews delivered by Murat while the TEXT-LAZY
pull runs. Review 3 = architectural #2 ("objective misaligned; pivot to a
capital-allocation engine"; phases A–F). Review 4 = short technical (praise +
one design question on the SignalDoc robustness arm). Also in scope: house
sign-off on the Opus session's AUDIT-CIK-BRIDGE execution. Adjudicated per
`EXTERNAL_RESEARCH_INTAKE.md`; **only these dispositions are binding.**

---

## 0. House sign-off on AUDIT-CIK-BRIDGE (module 64f86b4)

Endorsed as executed: audit ran BEFORE the result read, reported-never-deciding,
and it measured the right axes (dollar-volume deciles = the harness's own
segment axis; panel-death instead of the 0.4%-populated `dlstcd`). The finding —
survivorship neutrality holds at a shallow, monotone ~5pp gradient (died-in-panel
0.862 vs survived 0.913; worst cell 0.817) — supports interpretation with the
caveat the session already stated. One sharpening on how the caveat binds:
**the bias direction (missing names tilt toward the dead) can only flatter a
long-only book, so the caveat attaches to a PASS, not to a REJECT.** A marginal
explore pass must carry it verbatim; a rejection needs no discount from this
source. No bar moves either way.

## 1. Review 4 — the SignalDoc robustness-arm question (binds R12-B)

**Question asked:** run the Chen-Zimmermann construction variants as a unified
batch through the standard harness, or selectively isolate the highest-decay
factors to pinpoint construction differences?

**Ruling: unified batch. All 13 matched signals, SignalDoc's own construction,
byte-identical monthly factory harness (correction: the harness is monthly, not
daily), one pre-registered shot.** Selecting "highest-decay factors" would
condition the robustness test on the observed outcome — the −0.544 IS the
decay ranking, so isolating its extreme members is post-hoc selection inside
the very arm meant to test whether that ranking is a construction artifact.
The registered question is: does corr(|OSAP t|, |our t|) survive when OUR
construction choices are replaced by THEIRS on the same fixed 13? That requires
the full set, once. Per-signal construction forensics (where our PIT-strict
definitions diverge from SignalDoc's) are welcome AFTERWARDS as reported
diagnostics — never deciding, and never a reason to re-run.

## 2. Review 3 (architectural #2) — dispositions

**Header finding:** the review's phases A–F map ~90% onto the program that
already exists — A = the freeze (this round), B = REGIME-ANALOG phase 2
(queued), C = the R13 allocation program, E = the belief/evidence layer
(phase-2 spec), F = the forward-validation spine (live since June; defended
against the reset request on 07-27). Independent convergence banked. The ~10%
that differs is adjudicated below; one proposal is refused hard (registry
split), one is genuinely new and adopted as a design-note (capital flows).

| # | Claim | Disposition |
|---|---|---|
| 3.1 | "Objective misaligned: anomaly replication ≠ compounding better than SPY"; "prove you lose LESS in bad regimes" | **PARTIALLY CONVERGENT, no mission rewrite.** Goal A / Goal B have been split since 07-22. The factory was Goal A's necessary first phase and returned an honest, publishable answer. "Lose less in bad regimes while participating in good" is not a new mission — it is TSMOM-XA's registered decision rule (overlay maxDD ≥3pp shallower than control, Sharpe within −0.15, 24mo forward), live since 07-27. The demanded pivot is the already-sequenced R13. |
| 3.2 | Freeze; "maybe 20–30 high-conviction hypotheses, not 200" | **CONVERGENT** — freeze executes this round; post-freeze admissibility (prior_check + deflation + new-info-source exemption) already implements exactly this posture. |
| 3.3 | **Capital-flows research family** (ETF flows, passive share, dealer gamma/option positioning, Treasury issuance, buybacks, vol-target/CTA/risk-parity mechanical flows) | **ADOPTED AS DESIGN-NOTE — the one genuinely new research direction across all four reviews this round.** It legitimately fits the freeze exemption ("genuinely new information source" — flows are not another CRSP/Compustat characteristic transformation). Gate before ANY registration: a PIT-data feasibility audit per sub-hypothesis, because this family is where free data is thinnest: dealer gamma/positioning is vendor-locked; CFTC COT is free+PIT (futures only); ETF flow history partially free; 13F is quarterly+45d-lagged and the follower play is already CLOSED; buybacks (prstkc) are already on disk; net-liquidity (WALCL−TGA−RRP) is already tracked descriptively in the product. Route: one ranked design note → prior_check → at most 1–2 deflated registrations. No timeline commitment. |
| 3.4 | Microstructure as research ("when signals fail": liquidity shocks, spread widening, volume imbalance) | **PARTIAL / DESIGN-NOTE ONLY.** The cost half is done (KO invariance spreads live in the harness; that work is why the cost-killed cohort is credibly empty). The conditioning half — signal × liquidity-state — is the state×signal multiplicity bomb, same ruling as anomaly-timing (AI_PANEL_2026-07-28 §1.2). |
| 3.5 | Evidence graph → beliefs → probabilities; "the LLM should never own the truth, only explain it" | **ALREADY HOUSE LAW + PHASE-2 SPEC.** The BeliefState ledger carries exactly the structure sketched (belief, confidence, supporting/contradicting evidence, timestamps); the evidence-weight schema was adopted yesterday (28 §1.4); "LLM narrates, never allocates" has been canon since round 8. Graph infrastructure stays refused at ~50-doc corpus (round 6 ruling). |
| 3.6 | SELL signals as first-class research — "separate registry, separate evidence, separate confirmations" | **Family ADMISSIBLE post-freeze (re-affirmed); separate registry REFUSED HARD.** One registry with one cumulative deflation count is the integrity backbone of every claim this program makes — splitting registries resets the multiple-testing clock and is precisely how a shop launders its candidate count. Exits get first-class status INSIDE the same ledger, deflated against the same 158+. Receipts carried forward: calendar/hold-horizon exits answered (INSTR-HOLD-HORIZON: signal-band exits), ATR stop lane live; the untested part is cross-sectional deterioration triggers (revisions collapse, margin compression, guidance cuts) — a legitimate registrable family. |
| 3.7 | "Forward validation: run daily, never restart, never overwrite, timestamp everything" | **ALREADY THE HOUSE SPINE** — paper lanes (reconstruction-not-reset, 07-27), five IC clocks, belief ledger, PDUFA Brier ledger. Convergent; the 07-27 refusal to reset the paper accounts protected exactly the asset this review calls the most valuable. |
| 3.8 | NN for representation/clustering/regime detection, never price prediction | **CONVERGENT** — k-NN analog retrieval + episode clustering already are the representation layer; INSTR-VOC + round-8 unanimity bar the rest. Phase-2 descriptor expansion (breadth, sector leadership, dollar) noted as candidate features. |

## 3. Bindings

1. **R12-B (paper session):** the robustness arm runs as a UNIFIED batch of all
   13 matched signals under SignalDoc's own construction — no decay-ranked
   subsetting (§1).
2. **TEXT-LAZY interpretation:** the bridge-audit caveat attaches to a PASS,
   not a REJECT (§0).
3. **Any exits/SELL registration:** same registry, same cumulative deflation
   count — no parallel ledgers, ever (§3.6).
4. **Capital-flows family:** design note + PIT-data feasibility audit BEFORE
   any prior_check/registration (§3.3). Post-freeze.
