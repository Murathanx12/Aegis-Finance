# AI Panel — round 9 (2026-07-26): cost-model port executed

Two reviews (GPT-with-citations, Gemini), both fully aligned with the
standing queue — no new adoptions needed, no re-litigations attempted, no
hallucinated receipts this round. Verified: the cited "Century of Evidence
on Trend-Following" (Hurst-Ooi-Pedersen) and NMV taxonomy support the
allocator pivot already adopted; FundaPod/PIT-LLM citations match the
memory-infrastructure stance already in place (prior_check + registry now;
vector/graph at ~500 docs).

## Gemini's technical question, answered by the data

*"Corwin-Schultz high-low or Roll serial covariance?"* — **Neither is
feasible on our pull**: `dsf_monthly_agg` carries no daily high/low prices
(C-S dead) and no daily return series (Roll dead). It carries exactly the
input set for the **Kyle-Obizhaeva invariance spread** (one of the
TAQ-validated proxy classes in Chen-Velikov): per stock-month daily
volatility + daily dollar volume. Formula frozen verbatim from KO
(Econometrica 2016, eq. 33 + Table 6, fetched and quoted in the trial
doc): s/P = exp(−3.07)·σ·(W/W*)^(−1/3), W* = 800,000. Estimator chosen by
data feasibility, not preference.

## INSTR-COST-MODEL executed (module `TRIALS/INSTR-COST-MODEL.md`)

- Sanity gate PASS (largemid median half-spread 4.2 → 3.4 bps across eras,
  small > largemid — realistic TAQ-era levels, CRSP units confirmed).
- Regression guard PASS (flat-cost path byte-identical after the harness
  extension: banked 1.96/+23.2 reproduced exactly).
- **BRAIN-008 AFFIRMED and strengthened:** confirm t 0.89 → 1.11
  (gp_base) and 1.45 (gp_ead) under the half-spread primary; holds under
  full-spread stress (0.97/1.31). The flat 50 bps wall was
  over-penalizing the low-turnover small-cap book.
- **Cuts both ways (disclosed):** largemid realistic spreads (~3–4 bps)
  sit far below the flat 25 bps wall — cost-killed largemid rejects with
  real rank information (si_trend: ~14 bps/mo of relief) were penalized
  too hard. **INSTR-COST-REMEASURE-REJECTS is now admissible**: one
  pre-registered, deflated re-measure of the cost-killed shelf under KO
  spreads, AND-rule intact. Queued for decision — a new measurement
  condition is not re-litigation, but it gets one shot only.

## Queue (post round 9)

1. Decision: INSTR-COST-REMEASURE-REJECTS (recommend YES — one
   registration, then the shelf is settled under the better ruler).
2. JM2 + INSTR-REGIME-ANALOG (the allocation program).
3. Batch 10: 8-K event-flow (PIT-safe news) + exclusion screens.
4. Ceiling re-registration ~196 → factory freeze → paper.
5. Attended: TSMOM-XA seed; PDUFA scoring late Aug; conviction decisions.
