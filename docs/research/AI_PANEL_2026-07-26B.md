# AI Panel — round 8 (2026-07-26): four reviews of the project report + Build 3 executed

Reviews: Copilot, DeepSeek, GPT, Gemini — all reviewing
`docs/PROJECT_REPORT_2026-07-26.md`. Adjudicated against the ledger;
Build 3 (TRIAL-TGT-REBUILD) executed same session.

## Convergent verdicts (all four, accepted — they match the ledger)

- Do NOT build a neural network for trading signals (INSTR-VOC receipt
  cited by three of four). NN-for-regime-classification only ever as a
  pre-registered instrument; parked until JM2's simpler version is
  adjudicated.
- Finish the finite queue → freeze → paper → protect forward clocks →
  re-narrate product. The pivot as written stands.
- "Sell before the fall" is not a capability anyone has; trend-following
  sells AFTER and that is why it works (TSMOM-XA is defensive, not
  predictive).
- LLM never trades; perception/narration only (already CANON).

## Fact-checks (hallucinated receipts corrected — the reviews cite our
ledger back at us, so errors must be caught before they calcify)

| Claimed | Reality |
|---|---|
| DeepSeek: "our own INSTR-REVERSAL confirms negative Sharpe" | No such instrument. Real receipt: batch-1 st_rev/dip family, closed |
| DeepSeek: "GDELT/Social Sentiment tests came back dead (flat-to-negative)" | Sentiment backtests were never RUN — they are BARRED by PIT policy (no survivorship-clean historical text). Untested ≠ dead; the distinction matters for future claims |
| DeepSeek: signal-type survivor table (counts per class) | Invented breakdown; directionally fine, numbers not ours |
| GPT: "Layer 1 (stock selection) is mostly solved" | Overstated: two weak-positive survivors + one fusion is not "solved"; it is "adjudicated" |

## The round's genuinely new adoption — GPT's Layer-2 argument

GPT's core claim survives scrutiny: ~90% of factory effort went to
"which stock" (Layer 1), while institutional outperformance literature
locates most of the action in "what mix today" (Layer 2, allocation), and
our own receipts agree — the only macro instrument tested (TSMOM-XA)
survived while 150+ stock-pickers died. **ADOPTED as the post-freeze Brain
direction**, concretized as:

**INSTR-REGIME-ANALOG (queued, registration before any evaluation):**
- Build a daily historical state vector (data we already have or can get
  free: FRED-MD subset, VIX + term structure, HY-OAS credit spreads,
  yield curve, DXY, oil/gold, sector ETF relative strength, breadth) from
  ~1995.
- Deterministic k-NN similarity retrieval: "which 50 historical
  days/months most resemble today" → report the forward return/drawdown
  distribution of those analogs. LLM narrates the evidence; it never
  allocates.
- Phase 1 is DESCRIPTIVE infrastructure (like fragility/LPPLS — never
  arms). Any allocation rule derived from analogs is a separate
  pre-registered trial behind the explore/confirm wall, DSR-deflated.
- Fits every house constraint: auditable, no NN, no LLM-trades, and it is
  the "what world am I in" engine the panels keep asking for.

DeepSeek's INSTR-DYNAMIC-ALLOCATION draft (VIX 25/30, spread 2.5/3/4%
thresholds) is folded into the JM2 family rather than adopted verbatim —
its thresholds are arbitrary and each rule variant is a candidate;
JM2 + ANALOG first, composite rules after. GPT's 15-hypothesis list:
mostly regime-family variants → absorbed into the JM2/ANALOG program,
NOT another anomaly batch (GPT itself says stop those). Expert-switching /
mixture-of-experts: premature with 2 weak pickers + 1 overlay; revisit
post-JM2.

## Gemini's closing question — answered

Cost model vs CZ-CALIB first? **CZ-CALIB + HARNESS-VALID first.** A new
cost model applied through an unvalidated harness conflates two changes;
validate the ruler, then re-measure with better friction. (Also the
calibration pair is what makes the paper defensible.)

## Build 3 executed — TRIAL-TGT-REBUILD (#154-155): REJECT, family closed

Module `TRIALS/TRIAL-TGT-REBUILD.md`, NEG_RESULTS §17. Nominal
split-guarded rebuild (no adjustment arithmetic — the original sin).
Raw target upside strongly PERVERSE (largemid −90 bps/mo t −3.62; small
−199 t −7.21); PSZ low-dispersion conditioning moves the predicted
direction (−90 → −43.5) but never the sign (IC t −3.77). Confirm never
opened. Analyst-source pickers 0-for-3. Cumulative **155**.

## Standing queue (post round 8)

1. **INSTR-CZ-CALIB + INSTR-HARNESS-VALID** (validate the ruler; paper
   defensibility).
2. Chen-Velikov effective-spread cost model port (re-measure small-cap
   survivors under realistic friction).
3. **JM2 + INSTR-REGIME-ANALOG** — the allocation program (the round's
   adopted direction).
4. Batch 10 (long-side exclusion screens; insider role-weights) if slots
   remain sensible under the ceiling.
5. Overfit-ceiling re-registration ~196 → factory freeze → paper.
6. Attended (Murat): TSMOM-XA lane seed; PDUFA scoring late Aug;
   conviction decisions; quarterly refresh Oct.
