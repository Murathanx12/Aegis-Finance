# AI panel adjudication — 2026-07-29B (GPT, Gemini, DeepSeek on the session summary + next-session prompt)

Second round of the day. Murat pasted three external reviews of the
2026-07-29 session summary (D1–D4 verdicts, TRIAL-COND-VT REJECT, the
post-freeze prompt). Raw reviews: session transcript (manual workflow — no
panel_raw quarantine needed for pasted rounds). House rule applied
throughout: unverified numeric magnitudes are discarded; direction and
mechanism only.

Every claim below was checked against repo receipts before ruling.
Verification performed this session: `backend/services/factor_model.py:57`
(read), `Aegis module/docs/FREEZE_2026-07-28.md` + `aegis_brain/harness/
benchmark.py` (FF vintage receipts), `docs/research/DATA_STRATEGY_2026-07-28.md`
§4 (BBBY/Overstock), `NEGATIVE_RESULTS.md` §§4, 20, 21, AI_PANEL_2026-07-29.md
(round-1 rulings), live `aegis_verified_state` (all 10 lanes fresh, day 51),
and the Optimus restart check (garbage query → `no_match`; "freeze 158
candidates" → finance corpus only). Optimus hardening is CONFIRMED LIVE.

---

## ADOPTED (written into ROADMAP_2026-07-29_POST_FREEZE.md amendments)

1. **Acceptance criteria for EVENT-INTEL (DeepSeek #1).** Correct gap — the
   Priority A prompt said "build" without defining done. Adopted; concrete
   spec now in the roadmap amendments.
2. **Pre-committed direction taxonomy (DeepSeek #2).** Adopted:
   `EXPLICIT` (stated in source: beat/miss, M&A, guidance change) /
   `IMPLIED` (inferred from language, always tier-labeled) / `NEUTRAL` /
   `UNKNOWN` (unclassifiable, said so). Binding rule: direction is always
   *relative to the named scope entity*, never the market.
3. **D4 CLOSED BY DECISION (DeepSeek #5).** The measured state (return-sign
   corr 0.104/0.063 = channel absent; crash-outcome corr 0.204/0.259 =
   plausible only) does not support outcome-confidence anywhere. Ruling:
   **no outcome-confidence labels on any surface** until a successor engine
   validates the channel under the D3 harness. Extraction-quality tiers
   (item 4) are a different, allowed thing — fidelity of the parse, not
   probability of the outcome.
4. **Extraction-quality confidence tier (DeepSeek C).** Adopted:
   HIGH / MEDIUM / LOW / FAILED, defined against source text support.
   Labeled as extraction fidelity in the UI so it cannot be read as a
   forecast.
5. **"What the brain can say" playbook (DeepSeek A).** Adopted — an explicit
   allowed/forbidden statement list ships with EVENT-INTEL (allowed: "this
   occurred", "measured base rate is X% over Y, N=Z", "no measured base
   rate exists"; forbidden: "suggests/implies", any future-tense
   attribution, any buy/sell language). Concordant with
   TRACK_RECORD_POLICY.
6. **Historical shakedown before deploy (DeepSeek B).** Adopted: run the
   extractor over trailing stored feeds, manually audit 50 events against
   source text. Caveat bound in: this is descriptive QA of the *parse* —
   no return outcomes attached, so the LLM-vintage bar (A2 firewall,
   profit-mirage receipts) is not violated.
7. **Source integrity check (DeepSeek D) + /dev extraction stats
   (DeepSeek F).** Both adopted — cheap, and the dashboard slots into the
   existing private /dev surface.
8. **Clock check as a standing /go item (DeepSeek #4).** Adopted (1 min).
   Current state, so the next session doesn't re-derive it: forward record
   day 51 of ~730 — nothing can approach the 24-month line before mid-2028;
   nearest maturities are PDUFA ledger first scoring ~late Aug 2026,
   quarterly panel refresh ~Oct 2026, TRIAL-CMP-INSIDER-IC earliest
   decision 2027-07-21.
9. **Priority B concretized (DeepSeek #3).** Adopted — "skeleton" replaced
   with a named deliverable: a 3-page draft (intro + method + results
   shell) with a fixed exhibit list. Spec in roadmap amendments.
10. **Product-side Fama-French vintage pin (Gemini #1).** **The real catch
    of this round — VERIFIED.** `backend/services/factor_model.py:57-58`
    still fetches FF factors dynamically via `pandas_datareader`; only the
    research harness is pinned (parquet sha256 `54e3b8dd…`, vintage ≤
    2026-07-21, FREEZE doc). Our own measurement says French rewrites
    92.8% of HML months across one 18-month vintage step — so the
    product's factor attribution silently floats while the paper's
    doesn't. Adopted as a hardening build item: product reads the same
    pinned parquet + hash, fail-loud on mismatch, disclosed-stale rather
    than silently refetched.
11. **Canary assertions per feed (Gemini #2).** Adopted into EVENT-INTEL
    acceptance criteria. This is house doctrine already ("an empty 200 is
    not evidence of absence" — AI_PANEL_2026-07-28D); the adoption is
    making it a *per-feed startup assertion* rather than a review-time
    audit.
12. **"Data Artifact Graveyard" as a named Paper-1 section (Gemini #4).**
    Adopted. All five proposed exhibits verified real in our receipts:
    (a) 8-K placebo control t −11.33 vs treatment −7.06 (NEG_RESULTS §20);
    (b) FF vintage drift 92.8% HML / 91.5% SMB / 61.2% Mkt-RF
    (benchmark.py, FREEZE);
    (c) SEC quarterly-index retroactive rebuild vs daily-index PIT
    (DATA_STRATEGY);
    (d) yfinance serving Overstock prices under BBBY with the Bed Bath
    longName (DATA_STRATEGY §4);
    (e) the empty cost-killed cohort.
    One correction bound in: (e) is a **large/mid-cap** finding — "every
    rejection was informational" was measured on the non-contaminated
    large/mid set, not all 158.
13. **Optional event schema extensions (GPT, news-engine section).**
    Adopted as optional fields (actor, action, duration class, affected
    industries) on the typed event; `scope` remains the primary key.
    "Event understanding, not summarization" is exactly Priority A's
    framing — concordant.
14. **"Reasoning engine, not alpha engine" (GPT, closing).** Adopted as
    *paper framing language* — it names what the receipts already show
    (the graveyard + the honest-null belief engine + a descriptive
    product). Operationally it changes nothing: that IS the current
    roadmap. GPT's own caution ("evaluate every reasoning module as
    harshly as the factors") is already binding (D3 harness = house ruler).

## REFUSED with receipts

1. **Causal propagation chains / knowledge graph "as infrastructure"
   (GPT Programs 2 & 3, commodity/supply-chain/policy graphs).** Refused
   again — this is round-1's refusal at lower stakes, and the receipts
   don't move: our own measured edges are ~zero (THEME-SUPPLY B−A t 0.10;
   cust_mom REJECT; conn_mom net t −0.78 at 67% turnover; cust_conc sign
   reversal). A multi-hop graph multiplies coefficients measured at ~zero
   and adds degrees of freedom the zero-skill ceiling prices at t≈6.6
   from nothing. An unscored graph is unmeasured decoration with real
   maintenance cost. The *permitted* descriptive form survives inside
   EVENT-INTEL: a static scope→sector/commodity mapping on context cards
   (a lookup, not a propagation). If a future session wants graph
   *infrastructure*, it must first name the measured question the graph
   answers better than the lookup.
2. **Expectations engine / expectations database as new programs (GPT
   Programs 1 & 4).** Already ruled in round 1: this is the *most* tested
   ground on the wall (revisions running forward as T10; analyst-target
   family 0-for-3 with raw upside PERVERSE at t −3.6/−7.2; PEAD inverted,
   IC t −2.6). Most of "Program 4" already exists as built collectors
   (revisions, insider, options positioning, 13F, short interest). The
   IBES market-implied-expectation formulation stays QUEUED; it opens
   nothing under the freeze.
3. **Historical event replay, presented as novel (GPT).** Refused — it is
   the most-litigated ground in the repo. News replay is PIT-barred
   (NEG_RESULTS §4: no survivorship-clean free archive; news validates
   forward-only). Filings replay was done PIT-clean on EDGAR daily
   indexes and produced both control-arm kills (§20 8-K; TEXT-LAZY).
   LLM-on-historical-text without ChronoBERT-class vintage checkpoints is
   barred by binding rule + the profit-mirage receipts (~37% lookahead
   inflation). And "measure belief calibration through time" is the D3
   harness — built, and it already returned its verdict on the one belief
   engine we have.
4. **Decision science as a new pipeline (GPT Program 5).** Concordant
   direction, refused as framing: the allocation layer has NO cheap
   deflation budget (dead-list item — the confirm window holds ~1–2
   regime events). It is not a "pipeline"; it is one expensive registered
   shot at a time. Ledger: TSMOM-XA survived; jump model, JM2, and
   conditional VT died at confirm. That cadence continues.
5. **Gemini's base-rate context-card numbers.** The *design* is already
   Priority A (attach only measured stats, N-counts, "no measured base
   rate" default). But Gemini's exhibit — "Item 2.04, N=412, 5-day drift
   median −1.42% / mean −2.18%, your 2004–2024 WRDS dataset" — is
   **fabricated**. No such table exists anywhere in either repo; our only
   8-K measurement used 3-calendar-month windows and its apparent drift
   was refuted as selection (§20). Any base rate EVENT-INTEL ever
   attaches must be measured by us, carry its N, and disclose the §20
   selection trap for filing-conditioned cohorts.
6. **"Special situations" as a study area (GPT strategy list).** Nothing
   opens: 8-K family is unadjudicated-with-trap (successors need
   eligibility frozen pre-event), FDA is dead at both resolutions
   (§11, §16), and spin-offs/restructurings would be new registrations
   against the freeze. Post-freeze, this is paper material, not a scan.

## PANEL ERRORS logged (feed back next round)

1. **Gemini fabricated a data exhibit** (the N=412 Item-2.04 table,
   presented as our own WRDS measurement). Most dangerous error type — it
   wears the house's clothes. Discarded; rule reaffirmed.
2. **Gemini embellished the VT mechanism.** Entry-at-full-weight is right
   (receipt: 63-td vol 0.162 vs causal q80 0.202 on 2020-02-28). "It
   de-risked at the bottom and missed the historic rebound" is not in the
   measured record — the confirm failure was a Sharpe-bar miss (0.836 vs
   0.847) plus maxDD identical to SPY, at a 2.6pp CAGR cost. Direction
   fine; narrative invented.
3. **Gemini overgeneralized the empty cohort** ("158 candidates died from
   informational arbitrage rather than friction") — measured on large/mid
   only; small-cap rejections were frequently cost/turnover deaths.
4. **GPT presented historical replay as unsuggested** ("something I think
   nobody has suggested") — see REFUSED #3; it has been proposed,
   executed where PIT-clean, and barred where not, across ~4 rounds.
5. **GPT's scores (9.8/10 etc.)** — unfalsifiable, discarded per house
   rule. (Noted without offense; the prose diagnosis was useful.)
6. **DeepSeek: clean round.** Its 24-month-threshold worry is moot (day
   51; nothing matures before mid-2028) but was framed as "verify", which
   is correct procedure. Best-calibrated reviewer this round.

## Net effect

Priority A gains a full acceptance spec (criteria, taxonomy, tiers,
canaries, shakedown, playbook, dashboard); D4 is closed by decision; one
new hardening item (product FF pin) enters the build list; Priority B
becomes a concrete 3-page deliverable with a fixed exhibit list including
the Data Artifact Graveyard. No research reopens. The freeze holds at 159
cumulative candidates.
