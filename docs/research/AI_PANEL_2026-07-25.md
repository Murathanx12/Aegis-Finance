# AI panel — round 4 adjudication (2026-07-25)

Two reviews pasted by Murat (R1 = "signal taxonomy" review; R2 = "10
literature-backed hypotheses" review), adjudicated against the house ledger.
All load-bearing citations live-verified by a research agent same day (10/10
checked; verdicts below cite the verification). Standing rules applied:
nothing adjudicated is reopened; adoption = pre-registration, never belief.

## What both reviews got right (and the house adopts)

1. **Signal taxonomy (R1) — ADOPTED.** Every hypothesis now carries 4 tags at
   registration: information source, expected decay horizon, turnover class,
   role (stock picker | regime/screen filter | allocator). This extends the
   horizon-first rule (round 3) to a full schema. Artifact:
   `Aegis module/docs/SIGNAL_TAXONOMY.md` — the ranked map of signal
   FAMILIES the reviews asked for, seeded with all 146 candidates to date.
2. **"Ranked map of families, not a leaderboard of ideas" (R1)** — this is
   the correct reading of 146 candidates / 2 survivors. The taxonomy doc is
   that map; the combiner shelf (IC-real/net-dead class) is its core exhibit.
3. **Separate lanes for FDA daily-CAR and the daily macro harness (R1, R2)**
   — already the queue plan; confirmed.

## Adopted → registered TODAY (Batch 8, frozen `0297101` before any scan)

| Idea (source) | Registration | Verified prior |
|---|---|---|
| Short-interest TREND not level (R1#2) | `si_trend` (3m ΔSI/shares, long decreases) | Zhu-Duan-Tu AEF 2019 real but second-tier journal → MEDIUM; distinct from the CLOSED dtc/SI-level family |
| R&D/ME × gross profitability (R2#6) | `rd_gp` (RD/ME gated top-half GP) | CLS 2001 verified, survives HXZ; the GP conditioning is NOVEL — no published paper found (2026-07-25 sweep) |
| PEAD analyst+TS agreement (R1#5, R2 adjacent) | `pead_agree` (min z where both>0) | Livnat-Mendenhall JAR 2006 verified; large-cap decay 2010s documented → declared a-priori likely net-dead at our costs, scanned for the combiner shelf |

Plus **INSTR-OVERFIT-CEILING** pre-registered and run same day (R2's #1
execution step; was already queued): closed-family library, mining arms,
explore→confirm decay curve. Results in the module trial doc.

## Adopted → folded into ALREADY-QUEUED registrations (no new trials yet)

- **Target-price rebuild** (ibes_adj on disk): now carries (a) the
  **dispersion-conditioned design** — Palley-Steffen-Zhang, Mgmt Sci 2025,
  verified: consensus-implied return is informative at LOW dispersion and
  NEGATIVELY correlated with returns at high dispersion (incentive-driven
  staleness). Long-only leg for us: high implied return AND low dispersion.
  (b) R2's "de-biased TP drift ~1%/mo" corrected by verification: that
  magnitude is Da-Hong-Lee 2016 QUINTILE four-factor alpha (0.87–1.03%/mo),
  not a de-biased decile; Da-Schaumburg 2011 is 2%/mo on a 5-year window
  with known decay risk. Registered priors will use the corrected numbers.
- **FDA daily-CAR successor** (dsf_pharma on disk): R2#8's attention gate is
  adopted as a pre-registered SECONDARY arm with honest framing — the
  Chemmanur et al. 2026 result is patent-grant vs FDA salience; a within-FDA
  pre-event-volume sort is an extrapolation, so it is an exploratory arm,
  never the headline test. Econ Letters 2018 event-day misspecification
  warning goes into the CAR spec (market-model, day-0 excluded arm).
- **INSTR-REGIME-HMM spec AMENDED → statistical jump model** (R2#10):
  Shu-Yu-Mulvey (J. Asset Mgmt 2024) verified — explicit jump penalty
  enforces regime persistence vs HMM whipsaw. This matches the round-3
  panel answer; the daily-harness build will implement the jump-penalty
  variant as the primary spec, causal/expanding as already frozen.
- **NEW: INSTR-SBCORR** (R2#9) — stock-bond 60d rolling correlation as a
  regime gate (SPY/TLT dailies on disk). Registered as a DESCRIPTIVE
  instrument in the batch-4 macro family; joins the daily-harness queue.
- **BoardEx design prior WEAKENED** (R1#7): Larcker-So-Wang 2013 (+4.68%/yr)
  verified, but a recent Management Science paper finds the premium
  insignificant after systematic-risk controls — the alpha may be beta.
  BoardEx centrality stays queued as an information-transmission LAYER
  (taxonomy role: filter/combiner feature), not a standalone picker.

## Refused (receipts; re-litigation ban)

- **Net payout yield (R2#7):** tested in batch 7 — largemid t_net 0.80 /
  IC t 1.85, failed both legs; small IC-real/net-dead. Verification adds:
  Goyal-Welch-Zafirov RFS 2024 finds payout-type predictors weak OOS — the
  literature claim itself was overstated. CLOSED.
- **Inventory/receivables divergences = "channel stuffing" (R2#5):** batch 7
  `inv_div`/`rect_div` — IC real in small (t 2.6–4.0), net dead everywhere.
  Combiner shelf. CLOSED.
- **Conditional customer concentration (R1#6):** the conc family is closed
  BOTH directions including a confirm KILL (BRAIN-010, DSR 0.0003) — the
  proposed conditioning (switching costs, bargaining power) is a new coat on
  a fully adjudicated corpse. REFUSED.
- **Hold-horizon experiments (R1#10):** already answered —
  INSTR-HOLD-HORIZON: flat 14–17 bps/mo net across 1–24mo bands; sell on
  signal-band exit. Duplicate.
- **Insider cluster refinements (R1#1, partial):** clusters closed
  (BRAIN-009 — add nothing over single opportunistic buys vs the coverage
  universe). The UNTESTED remainder — role weights (officer vs director) —
  is a legitimate batch-9 stub on the flag harness, weak prior.
- **GPR overlay / TSMOM (R1#8, #9):** already registered (INSTR-GPR-EVENT,
  INSTR-TSMOM-XA). No-op.

## Deferred / blocked

- **Director-interlock supply-chain spillover (R2#3):** the supplier
  comovement thesis is DEAD here (both arms, §12). An interlock-conditioned
  EVENT-WINDOW design is a genuinely different mechanism (information
  conduit), so it is admissible — but only as a BoardEx phase-2 event study
  with its own registration, and the freeze must state explicitly why it is
  not the killed thesis. Weak prior.
- **Executive option hoarding (R2#4):** needs Execucomp — entitlement
  unverified. WRDS want-list for the next attended Duo session.
- **Analyst herding distance (R1#4, part):** needs `ibes.recddet` — already
  on the want-list.

## Corrections to the reviews (for the record)

- R2's "Total Net Payout Yield … consistently survives post-2020" is
  contradicted by both our batch-7 run and Goyal-Welch-Zafirov 2024.
- R2's "~1%/mo for highest de-biased information decile" conflates 2–3
  papers (see tgt-rebuild note above).
- R2's FDA low-attention claim over-reads Chemmanur et al. (their design's
  low-salience arm is patent grants, not low-attention FDA approvals).
- R1's read of our DTC result ("real signal, wrong monetization") is
  accepted — it is exactly the combiner-shelf/filter reclassification the
  taxonomy now encodes.

Scoreboard after this round: cumulative explore candidates **146** (batch 8
registered), survivors unchanged (BRAIN-003 insider, BRAIN-008 gp-small,
BRAIN-007 fusion), 8 live forward lanes.
