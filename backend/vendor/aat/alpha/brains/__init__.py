"""EMPTY STUB -- deliberately NOT a copy. See `backend/vendor/__init__.py`.

The execution repo's `alpha/brains/__init__.py` imports the whole brain fleet.
Only `base.Forecast` is needed to validate a `Thesis`, so this package exposes
nothing and imports nothing. `test_human_thesis.py` asserts this file is empty
of imports, so the difference stays deliberate instead of drifting.
"""
