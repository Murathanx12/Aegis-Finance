# INVALID — do not cite: leaderboard_2026-09-26T153843Z.json (and run_/top10_ of the same run id)

This run scored every fundamentals rule on a **25-company** facts table. At 23:16 HKT the XBRL
re-extraction (`scripts/pull_sec_fundamentals.py --universe-from-bars`) was run WITHOUT
`--limit 3000` (the default is 25) and it rebuilt `fundamentals_sec/sec_facts_history.parquet`
from 3,000 names down to 25. Symptoms on this receipt: 7 fundamentals rules refused with
"no breadth cell had k selectable names"; `rd_intensity` +148.6% on the 2024-26 window with
`n_dev_months 0`; `pricing_power_unwatched` / `rev_accel_margin` dev vs SPY −0.09 → +0.44; the
dev-selected top-10 line moved from +8.8 pp (T150811Z) to +15.1 pp. The last valid receipt is
`leaderboard_2026-09-26T150811Z.json`; the date-named "latest" copies were restored from it.
The wide extraction was relaunched 23:46 HKT (`fundamentals_sec/pull.pid`); the factory must be
re-run after it finishes and only then may the intangibles rows be read.
