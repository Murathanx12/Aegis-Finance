Committed: `3b692db`.

---

## Honest Self-Assessment

**What went right:**
- Found and fixed 4 real bugs that were provably broken (wrong arg count, nonexistent method, `.get()` on dataclass)
- 53 tests with boundary coverage — every threshold boundary is tested at the exact value
- Narrowed 9 broad excepts to specific tuples, so real bugs like these won't hide again
- Config extraction makes thresholds tunable without code changes

**What I'm not sure about:**
- I never traced whether `signal_engine.get_market_signal()` actually converts `external_consensus="BULLISH"` into a nonzero numeric score. The 13% weight is in config, but if the code path that maps string → number is broken, the fix changes nothing visible to users. Next cycle should verify this.
- The sector.py fix adds a `fetcher.fetch_fred_data()` call — I didn't verify whether this is cached or doubles FRED API load.

**What I'd do differently:**
- Run a live integration test (`GET /api/signal`, check `components.external != 0.0`) instead of relying only on unit tests
- Wire `drift_detector.py` or `return_model.py` to endpoints — higher user-facing impact than 53 tests for one service
- Verify the signal_engine code path FIRST before claiming the fix "unblocks" external consensus

**Depth rating: MEDIUM** — Fixed real bugs and hardened the code, but didn't verify the end-to-end signal flow or tackle the dead services.

---STDERR---
